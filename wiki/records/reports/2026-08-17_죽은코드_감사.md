## 후보 2개

정적 감사 범위: `app/**/*.py`의 모듈, top-level 함수·클래스·모듈 수준 변수 및 `APIRouter` 선언. 참조 검색에는 `app/**`, `tests/**`, `docs/**`, `scripts/**`, `config/**`를 포함했다. `__init__.py`, `conftest.py`, `app/presentation/api/app.py`는 사용자 지시대로 모듈 미사용 판정에서 예외 처리했다.

### 1. `app/core/context.py:40` — `NEVER_EVICT`
- 무엇: ContextPack 구성에서 절대 제거하지 않을 섹션을 나타내도록 선언된 모듈 수준 상수.
- 근거: 다음 실제 검색은 저장소 전체(`app`, `tests`, `docs`, `scripts`, `config` 포함)에서 정의행 외 참조를 찾는다.

  ```powershell
  rg -n --glob '*.py' --glob '*.md' --glob '*.yaml' --glob '*.yml' '\bNEVER_EVICT\b' .
  .\app\core\context.py:40:NEVER_EVICT: frozenset[str] = frozenset({"system_instruction", "case_state"})
  ```

  출력은 위 정의행 1건뿐이며, 정의행 외 출력은 0건이다. `importlib.import_module`, `__import__`, `implementation_ref` 기반 동적 참조 검색에서도 이 이름을 조립하는 경로는 확인되지 않았다.
- 확신도: 높음

### 2. `app/presentation/ui/routes.py:46` — `_legacy_page`
- 무엇: 이전 `/ui/**` 콘솔 화면용으로 보이는 별도 HTML 렌더 함수.
- 근거: 다음 실제 검색은 저장소 전체(`app`, `tests`, `docs`, `scripts`, `config` 포함)에서 정의행 외 참조를 찾는다.

  ```powershell
  rg -n --glob '*.py' --glob '*.md' --glob '*.yaml' --glob '*.yml' '\b_legacy_page\b' .
  .\app\presentation\ui\routes.py:46:def _legacy_page(title: str, body: str) -> HTMLResponse:
  ```

  출력은 위 정의행 1건뿐이며, 정의행 외 출력은 0건이다. 현재 `app/presentation/ui/__init__.py`는 `tenant_router`와 `composer_router`만 `include_router`하며, `_legacy_page`를 동적으로 조회하는 경로도 확인되지 않았다.
- 확신도: 높음

## 제외한 확인 결과

- `app/`의 모듈 미사용 후보: 0개. `__init__.py`, `conftest.py`, `app/presentation/api/app.py` 예외를 적용하고 정적 import를 대조했다.
- 라우터 미연결 후보: 0개. `tenant_router`는 `app/presentation/ui/__init__.py:33`에서, Composer UI `router`는 `app/presentation/ui/__init__.py:37`에서, API Composer `router`는 `app/presentation/api/app.py:58`에서 연결된다.
- `FeedbackAnalyticsTeam`은 `config/project.yaml`의 `implementation_ref`로 동적 import되므로 후보에서 제외했다. `app/composition.py`와 `app/core/project_config.py`에 `importlib.import_module` 구현이 있다.

## 검증

실행 명령:

```powershell
python -m pytest -q
```

결과: `331 passed, 3 failed, 1 deselected`.

실패한 3건은 `tests/integration/rag/test_rag_integration.py`의 OpenAI API 호출이 현재 환경에서 `api.openai.com` 네트워크 차단(`PermissionError: [WinError 10013]`)으로 실패한 것이다. 감사 중 `.py`·설정·기존 문서 파일은 수정하지 않았고, 이 리포트 파일만 새로 생성했다.
