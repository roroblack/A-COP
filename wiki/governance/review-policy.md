---
type: policy
title: 리뷰 정책
description: 누가 무엇을 검토하고 draft를 stable로 어떻게 올리는가. 6명이 동시에 써도 충돌하지 않게 하는 규칙
status: draft
tags: [governance, documentation]
---

# 리뷰 정책

## 소유 원칙

**문서 하나에 소유자 한 명.** 공동 소유는 최대 2명까지만 허용한다. 셋 이상이면 아무도 안 고친다.

소유자는 front matter의 `owners`에 적는다.

```yaml
owners: [human:서유현]
```

소유자가 하는 일은 셋이다.

1. 내용이 낡았는지 판단한다
2. 다른 사람의 수정을 검토한다
3. `draft` → `stable` 승격을 결정한다

## 영역별 소유

| 영역 | 소유 |
|---|---|
| `product/` | 기획 담당 |
| `business/` | 기획 담당 |
| `architecture/` | Core 담당 |
| `delivery/` | 팀 리드 |
| `evaluation/` | 평가 담당 |
| `research/` | 조사한 사람 |
| `decisions/` | 결정한 사람 |
| `governance/` | 팀 리드 |

`[미확보]` 실제 담당자 이름은 팀에서 배정한 뒤 채운다.

## draft → stable 승격

`stable`은 **"믿고 인용해도 된다"**는 뜻이다. 다음을 전부 만족해야 올린다.

| 조건 | 확인 방법 |
|---|---|
| front matter 필수 필드가 채워짐 | CI가 검사 |
| 모든 `[실측]`·`[외부]` 주장에 근거가 연결됨 | 소유자 확인 |
| `[미확보]`에 "무엇이 필요한지"가 적힘 | 소유자 확인 |
| 소유자 아닌 1명이 읽고 이해함 | 리뷰어 승인 |
| 인접 문서로 링크가 걸림 | CI가 링크 검사 |

**이관해 온 문서는 무조건 `draft`로 시작한다.** 옛 문서가 새 구조에 들어왔다는 이유로 권위가 생기면 안 된다.

## 리뷰가 필요한 것과 아닌 것

| 변경 | 리뷰 |
|---|---|
| 새 `decision` 문서 | **필수.** 결정은 되돌리기 비싸다 |
| `contract` 문서 수정 | **필수.** 다른 저장소가 의존한다 |
| `stable` 문서의 결론·수치 변경 | **필수** |
| 불변식 추가·삭제 | **필수.** 테스트와 짝이라서 |
| `draft` 문서 작성·수정 | 불필요 |
| 오탈자·링크 수정 | 불필요 |
| `index.md`에 항목 추가 | 불필요 |

## 충돌 방지

**하나 — 한 문서는 한 사람이 고친다.** 동시에 고쳐야 하면 먼저 말한다.

**둘 — 새 문서는 `index.md`에 먼저 등록한다.** 제목과 `description`만 올려두면 다른 사람이 중복 작성하지 않는다.

**셋 — 큰 문서를 쪼갤 때는 미리 알린다.** 파일이 사라지면 남의 링크가 깨진다.

## 낡음 판정

`stale_after` 같은 자동 만료는 지금 쓰지 않는다. 이관 직후 수백 개가 한꺼번에 만료로 뜨기 때문이다.

대신 사람이 판정한다. 다음 세 시점에 확인한다.

| 시점 | 확인 대상 |
|---|---|
| 마일스톤 직전 | `delivery/`, `product/` |
| 계약 변경 시 | 그 계약을 참조하는 모든 문서 |
| 근거 등급이 바뀔 때 | 그 수치를 인용한 문서 |

**근거 등급이 `[추정]`에서 `[실측]`으로 바뀌면 결론이 바뀔 수 있다.** 그때는 인용한 문서를 전부 확인하고 `log.md`에 적는다.

## ★ [2026-09-01 추가] 정합성 점검 주기

`[실측]` `_문서관리_전략.md`에서 이관. **이 절이 빠져 있었다.**

```
2 · 4 · 6 · 8 · 10 주차
```

정본·코드·제출물의 **반복되는 사실**을 대조한다. 무엇을 세는지는 정해져 있다.

| 대조 항목 |
|---|
| 변경 경로 |
| 기준선 링크 |
| **Team 집합** |
| **DoD 수** |
| **존재하지 않는 절 번호** |

불일치는 다섯 칸으로 기록한다.

```
정본 / 충돌 문서 / 판정 / 확인자 / 날짜
```

**"확인자"가 있는 게 중요하다.** 누가 봤는지 없으면 다음 사람이 다시 본다.

### 왜 이 다섯 가지인가 — 실제로 다 났다

`[실측]` 과거에 난 드리프트다.

| 사례 | 무엇 |
|---|---|
| **존재하지 않는 `§337` 참조** | 절 번호가 살아남아 인용됐다 |
| **Response 팀 누락** | Team 집합이 문서마다 달랐다 |
| **옛 팀명 병존** | 새 이름과 옛 이름이 같이 돌았다 |
| **DoD 18 → 28 → 29 드리프트** | 항목 수가 문서마다 달랐다 |

**넷 다 "세면 잡히는" 종류다.** 그래서 점검 항목이 세는 것들로 돼 있다.

`[실측]` 넷째는 지금도 유효하다 — 루트 `CLAUDE.md`가 **DoD 1~29**를 기준선으로 못박고 있다.

### ★ [2026-09-06] 정본 포인터가 한 칸 비어 있다

`[실측]` 루트 `CLAUDE.md`는 기준 사실 표 아래에 **"이 표가 오래됐으면 `program/research/index.md`가 정본이다"**라고 적는다. 그런데 지금 `index.md`(정리 기준일 2026-08-19)에는 **그 "현재 기준 사실" 표가 없다.** `_정본대조_2026-08-18.md`가 "index.md의 '현재 기준 사실' 표 ↔ v8 대조, 전부 일치"라고 적어 둔 걸 보면 08-18엔 있었고 08-19 정리에서 사라진 것이다. index.md의 "현재 근거" 표는 v8을 정본으로 가리킬 뿐 Team 목록·DoD 수·날짜를 다시 적지 않는다.

```
루트 CLAUDE.md  →  research/index.md  →  v8 본문
      "index가 정본"        "v8이 정본"      ← 실제 대조 대상
```

**index.md를 거치는 한 칸이 비어 있다.** 실무상 대조 대상은 v8 본문이고, 이 wiki의 [dod.md](../delivery/dod.md)·[timeline.md](../delivery/timeline.md)가 그 값을 든다. `[미확보]` 루트 `CLAUDE.md` 문장을 "v8 §0·§27이 정본"으로 고칠지, index.md에 표를 되살릴지는 정해지지 않았다 — 원본 수정은 이 wiki 범위 밖이라 [open-items](../delivery/open-items.md)에 올렸다.

`[실측]` 같은 index.md의 점검 캘린더가 `../wiki/governance/migration-scope.md`를 가리키는데 **그 파일은 폴더로 승격돼 없다**(→ [known-weaknesses.md](known-weaknesses.md)의 승격 사고). 죽은 링크다.

### 같은 종류의 사고가 이미 두 번 더 있었다

`[실측]` 원본 두 건에서.

| 언제 | 무엇 | 종류 |
|---|---|---|
| 2026-08-18 `_정본대조` | cs·sample `CLAUDE.md`의 §0은 v8을 가리키는데 **§7 문서 목록은 `../A-COP_구현계획서_v6.md`(죽은 링크)**를 가리켰다. 같은 파일 안 두 절이 다른 기준선 | [drift-case-voc.md](drift-case-voc.md)와 같은 "한 문서 안 두 절의 모순" |
| 2026-08-19 `_정합성점검` | 문제 8건 — 브리핑 HTML의 Team 분리 카드, index.md의 병합된 원본 파일명 참조, v8:407의 없는 HTML 참조, v8 §0-2 절 색인 누락, Composer v3 문서군 미등재 | 권고 5건 중 **5번만 이 wiki가 추적한다**([open-items](../delivery/open-items.md) B-1·C-1). **1~4의 처리 여부는 기록이 없다** `[미확보]` |

**셋 다 "있는가"가 아니라 "서로 맞는가"에서 났다.** 위 검사 셋과 `check_wiki.py`가 못 잡는 이유와 같다.

## 병합 규칙 다섯

`[실측]` 같은 문서에서 이관. **[migration.md](migration.md)의 절차와 짝이다.**

1. **변경 전 정본과 보존본을 먼저 확인한다.**
2. 새 내용을 **보완·중복·모순**으로 분류한다.
3. **보완만 정본에 추가한다.** 중복 문장은 만들지 않는다.
4. **모순은 사람 확인 항목으로 남긴다.** 최신 파일이라는 이유만으로 덮어쓰지 않는다.
5. 절 번호·Team명·DoD 수·일정·Docker 단계는 **실제 정본 heading/표와 대조한다.**

**4번이 이 wiki 를 만드는 내내 지켜야 했던 규칙이다.**

### ★ 그 4번을 이 문서에 적용한다 — 폐기된 결론 하나

`[실측]` `_문서관리_전략.md`(2026-08-18)의 결론이 **지금 하는 일과 정면으로 모순된다.**

> **전면적인 OpenWiki식 파일 분할보다** `research/index.md`를 현재 근거의 얇은 입구로 **강화한다.**
>
> 파일을 늘리는 것만으로 정합성 비용이 사라지지 않는다.

**우리는 정반대로 했다.** 파일을 나눈 wiki 를 만들었다.

| | 8-18 판정 | 9-01 실제 |
|---|---|---|
| 방식 | index 강화 | **파일 분할 wiki** |
| 이유 | 분할 비용 > 효과 | 사용자 지시 |

**규칙 4대로, 최신이라는 이유로 덮어쓰지 않고 남긴다.**

`[실측]` **다만 8-18 의 경고는 틀리지 않았다.** "파일을 늘리는 것만으로 정합성 비용이 사라지지 않는다"는 이 wiki 에서 그대로 확인됐다 — [type-verification/](type-verification/) 7회, 누락 6건. **분할이 정합성을 공짜로 주지 않는다.**

**차이는 하나다. 우리는 [check_wiki.py](migration.md)로 세는 장치를 같이 만들었다.** 8-18 에는 그게 없었다.

## ★ [2026-09-03] 모순은 도구가 찾는다

`[실측]` **`program/scripts/check_drift.py` 가 이미 있는데 wiki 에 0건이었다.**

```bash
python program/scripts/check_drift.py            # 전체
python program/scripts/check_drift.py --only 1   # 한 검사만
```

**기준선 문서의 개정이 일부 절에만 반영되고 다른 절이 옛말을 유지하는 상태**를 찾는다. → [drift-case-voc.md](drift-case-voc.md)

### 검사 셋

| # | 무엇 |
|---|---|
| 1 | **개정 표 반대진술 스윕** — 항목 20건. 개정된 항목의 반대 진술이 본문에 남아 있나 |
| 2 | 절 간 대립 |
| 3 | **코드 대조 (AST)** — 주석·docstring 은 세지 않는다 |

`[실측]` **3번이 AST 인 게 중요하다.** 문자열 검색은 주석에 낚인다.

### 지금 돌리면 6건이 나온다

```
외부 REST              표시없음 1곳   ★확인
VOC & Store Manager    표시없음 1곳   ★확인
A2A 검증               표시없음 7곳   ★확인
VOC 소관               표시없음 2곳   ★확인
```

**오늘 내가 손으로 찾은 VOC 모순을 이 도구가 이미 짚고 있었다.**

### ★ 판정하지 않는다

> **이 도구는 모순 후보를 찾아 줄 뿐 어느 쪽이 맞는지 판정하지 않는다.**

**"v7 이 뒤집은 근거가 없다"를 찾는 일은 사람 몫이다.**

### 아직 안 보는 것 — 그리고 그게 곧 문제가 된다

`[실측]` 도구가 스스로 적어 뒀다.

> **`v8 ↔ wiki/` 대조는 아직 안 본다.** 이관이 끝나면 v8 과 wiki 페이지가 **같은 내용을 두 벌** 갖게 되므로, **이번과 똑같은 드리프트 면이 새로 생긴다.**

**이 경고가 오늘 이미 현실이 됐다** — `core-vs-team.md` 가 v8 §7 재판정을 안 따라갔다. **wiki 안에서 난 것이라 이 도구도 못 잡았다.**

`[미확보]` **검사 4(v8 ↔ wiki)를 언제 붙일지 안 정했다.** [cutover.md](cutover.md) 와 같이 정해야 한다.

## ★ [2026-09-05] 다른 구현체와 대조하면 결함이 찾아진다

`[실측]` DoD 재검증 중 같은 방법이 **다섯 번** 결함을 잡은 걸 확인했다. `final_project_cs`를 `final_project_sample`(같은 계약의 다른 구현)과 대조하는 것이다.

| 언제 | 어디서 | 무엇을 찾았나 |
|---|---|---|
| DoD-03 | `agent_runs` 동시 시작 | 앱 레벨 `SELECT FOR UPDATE`가 **이미 있는 행만** 잠가서, 활성 run 0개일 때 두 요청이 동시에 insert에 성공할 수 있던 TOCTOU 레이스 |
| DoD-12 | outbox dedupe 제약 | `UNIQUE(topic, dedupe_key)`에 `tenant_id`가 없어 다른 테넌트끼리 충돌할 수 있던 결함 |
| DoD-27 | A2A `_call_within_deadline` | deadline이 루프 반복 사이에서만 확인돼, 원격 호출 한 번이 hang하면 훨씬 넘길 수 있던 갭 |
| DoD-14 | Composer JWT 시크릿 | `= ""` 기본값이 **fail-open** — 빈 문자열로 서명한 위조 JWT가 검증을 통과했다 |
| DoD-14 | Composer 쓰기채널 | 구현체 allowlist가 없어 `implementation_ref`로 **임의 모듈 import**가 가능했다. 위와 결합하면 인증 우회 체인 |

**다섯 다 같은 성격이다.** 단위 테스트는 통과하는데 **동시성·경계·타이밍·안전하지 않은 기본값**이 걸린 결함들이고, `final_project_cs` 코드만 읽어서는 "이게 문제일 수 있다"는 의심 자체가 생기기 어렵다. 특히 뒤의 둘은 **sample이 이미 막아 둔 것을 cs가 빠뜨린** 경우라, 대조하지 않았으면 발견할 계기 자체가 없었다. 같은 계약을 두 번 구현하면, 한쪽이 다르게 짠 지점에서 "왜 다르지?"라는 질문이 저절로 생긴다.

**이 프로젝트에서 sample을 "먼저 검증하는 참고 구현체"로 유지하는 이유([CLAUDE.md](../../CLAUDE.md) 최상위)가 여기서도 증명된다** — Composer 쓰기채널 검증([D-006](../decisions/D-006-composer-ownership.md))뿐 아니라 **동시성·경계 결함 발견 도구**로도 쓰이고 있다.

`[미확보]` **이 대조를 정기적으로 돌리는 절차는 없다.** 지금까지는 각 DoD 재검증·세션 작업 중 우연히 나온 것들이다. 언제·무엇을 대조할지 정해 두면 더 체계적으로 잡을 수 있다.

## 기록이 없는 날은 비워 둔다

`[실측]` `program/research/_일일작업_2026-08-26.md`. 8월 24일 저녁부터 27일 밤까지 커밋 0건인 사흘이 있었고, 그날 문서는 **"이 워크스페이스에는 이날의 기록이 없다"**로 시작해 확인한 것(커밋·파일 수정 시각·파일 이름)과 확인 명령만 적고 본문을 비워 뒀다.

> 없는 것을 추측으로 채우면 그건 지어낸 것이 된다.

[evidence-grading](document-standard.md)의 `[미확보]`와 같은 규칙을 일지에 적용한 것이다. 회의·조사·다른 도구에서의 작업은 저장소에 안 남으므로, 있으면 **그 사람이 증거 경로와 함께** 직접 적는다.

## `log.md`

영역별이 아니라 **저장소별로 하나**만 둔다. 여러 개면 어디에 적을지 고민하게 되고 결국 아무도 안 적는다.

적는 기준은 [../log.md](../log.md) 머리에 있다. 오탈자는 적지 않고, 결론·수치·`status`·소유자가 바뀌면 적는다.

## 관계

- [document-standard.md](document-standard.md) — 문서 작성 규칙
- [front-matter.md](front-matter.md) — `status`와 `owners` 필드
- [../log.md](../log.md) — 변경 이력
