---
type: guide
title: 사용자 실행 시트
description: 사람이 실행하거나 결정해야만 넘어가는 것. 2026-09-09 에 전부 다시 실측했다
status: draft
tags: [governance, documentation]
owners: [human:미배정]
domain: neutral
domain_note: 실행·결정 시트다. 항목의 도메인은 각 항목이 가리키는 문서가 말한다
---

# 사용자 실행 시트

`[실측 2026-09-09]` **이 시트를 전부 다시 쟀다.** 이전 판(2026-09-06)은 **끝난 일을 시키고 있었다** — 아래 §9.

명령은 **PowerShell 기준**이다(Windows PowerShell 5.1 에는 `&&` 가 없다). 끝난 줄은 `[x]` 로 바꾼다.

## 1. 결정 대기 — 가격 하나, 그것도 위임됐다

`[실측 2026-09-10]` v11 §0-3 에 남은 것은 **가격 하나**다 — 서울 1도시 상품의 값과 확정·조정 기준. **팀장이 계획 담당에게 위임했다**(Codex 와 정한다, §11-A). 사용자가 따로 정할 것은 없다.

`[실측]` **v11 §0-3 이 1주차로 잡은 둘 다 답이 나왔다.** → [D-016](../decisions/D-016-scope-narrowing-reasons.md)

| # | 물음 | 답 |
|---|---|---|
| ~~1~~ | ~~MVP 대상 도시~~ | **서울** (2026-09-10). 기술 제약이 아니라 **검증 주장의 분모**를 만드는 일이다 |
| ~~2~~ | ~~지원 언어 범위~~ | **한국어 하나** (2026-09-10, v11 §0-4). **고객 언어 번역은 고객 에이전트 몫**이다 — MVP 절삭이 아니라 우리 층의 일이 아니다 |

### 도시가 정해져서 시작될 수 있는 것들

**기다리던 것이 풀렸다.** 아래는 문서 세션 몫이 아니라 조사·구현 몫이다.

| 무엇 | 누구 |
|---|---|
| **액티비티 운영 변경 정보 출처 표본 조사** (서울권) | 조사 — v11 §0-3 2주차. Activity Team 의 가장 큰 `[미확보]` |
| 취소·환급 규정 표본 수집 | 조사 — 골프장 우천 위약금 규정부터 |
| 막차 데이터 확인 | Mobility 담당 |
| 여행 골든셋 시나리오 (서울 실장소) | **아직 아무도 안 맡았다** — 아래 §5 |

`[미확보]` **개인 에이전트가 실제로 번역을 해 주는지 확인하지 않았다.** 시연에 붙일 에이전트 1종으로 직접 확인해야 한다 — D-016 의 유일한 검증 항목이다.

## 2. 푸시 — 로컬 커밋 19건

`[실측 2026-09-09]` `role-core1` 이 `origin/role-core1` 보다 **19 커밋** 앞서 있다.

- [ ] 실행 전 — 다른 세션의 미커밋 파일은 스테이징하지 않는다

```bash
git push origin role-core1
```

거부되면(non-fast-forward) 억지로 밀지 않는다.

```powershell
git pull --rebase origin role-core1; if ($?) { python program/scripts/check_wiki.py }; if ($?) { git push origin role-core1 }
```

## 3. 코드 세션에 넘어간 것 — 보고만 받으면 된다

`[실측 2026-09-09]` **문서 세션이 할 수 없고 코드 담당 몫이다.** 상세는 [open-items.md](open-items.md).

| 항목 | 왜 급한가 |
|---|---|
| **★여행 Case 가 라우팅되지 않는다** | 여행 Team 여섯이 등록됐는데 분류기 어휘가 쇼핑몰이라 **아무도 안 불린다.** 이게 안 풀리면 시연할 게 없다 |
| **구현 카탈로그를 손으로 유지하는 목록 둘** | 새 Team 을 붙일 때 코어 파일 둘을 손으로 고쳐야 한다. 안 고치면 조립은 뜨는데 **Composer 로 저장이 안 된다.** 안은 나왔다 → [D-015](../decisions/D-015-implementation-catalog.md) |
| **`business_subject` 3단 폴백** | `travel_ops/_base.py:169` 이 `case_id` 로 떨어진다. **한 사건이 여러 예약을 바꿀 때 둘째가 조용히 합쳐진다.** ★**사양은 정해졌다**(v11 §4-E) — 구현이 남았다. **DoD-24 가 이걸 검사한다** |
| ~~도메인 무관 가드가 여행 어휘를 모른다~~ | **닫힘 (2026-09-10).** `booking`·`itinerary`·`lodging`·`supplier_booking`·`traveller` 가 들어갔고 옛 어휘는 안 지웠다 |
| 분류기 정렬 가드가 헛돈다 | `test_feedback_intent_alignment.py` 가 등록조차 안 된 팀 하나를 본다. **등록표를 읽어야 한다** |

## 4. 계획서 담당에게 넘어간 것

| 항목 | |
|---|---|
| ~~`business_subject` 규칙을 §6 에 넣기~~ | **닫힘 (2026-09-10).** v11 이 **§4-E 를 신설해** 정했다. DoD-24 로 검사 항목도 됐다 |
| ~~도시·언어를 좁히는 이유를 넣기~~ | **닫힘 (2026-09-10).** v11 §0-4 가 팀장 결정 여덟으로 적었다 |
| **남은 것은 가격 하나** | `[정정 2026-09-10]` 「남은 셋」이었다 — 통지 수신 확인은 결정 9(링크가 정본)로, 2도시 대 1도시는 결정 12 로, 직접 경로의 언어는 결정 11 로 닫혔다(v11 §0-4) |
| 여행 MVP 설계 결정 4건 | 정규화 칸 · 감시 소스 설정 위치 · 알림 수신 주체 · 기상 API 실연동 여부 → [open-items.md](open-items.md) |

## 5. 여행 골든셋 — 아직 아무도 안 맡았다

`[실측 2026-09-09]` `eval/datasets/golden.jsonl` 72건이 전부 쇼핑몰이고 **여행 시나리오는 0건**이다.

★**최종발표의 주장이 여기 걸린다.** "믿을 수 있다"는 측정으로만 서는데 측정할 데이터가 없다.

- [ ] 여행 골든셋을 **누가 만들지** 정한다
- [ ] 몇 건 만들지 정한다 (쇼핑몰은 golden 60 + holdout 20 이었다)

## 6. 문서 쪽에 남은 것 — AI 가 한다

`[실측 2026-09-09]` `python program/scripts/check_wiki.py` 가 정본이다.

| 항목 | 상태 |
|---|---|
| 도메인 축 표시 | **252/252 = 100%** |
| 판올림을 못 따라간 문서 | **0건** (2026-09-09 정리 완료) |
| 첫 표시의 정확도 | `[미확보]` **초벌은 낱말 세기로 냈다.** 손으로 판정한 것은 50여 개다 → [../governance/domain-axis.md](../governance/domain-axis.md) |
| 여행 조사 이관 | `program/research/_최종주제_후보비교_v4_2026-09-08.md`(385줄)가 wiki 에 안 들어왔다 |

## 7. 발표 전에 결론이 나야 하는 것

`[실측]` v11 §0-3 · §11.

| 항목 | 언제 |
|---|---|
| ~~도시 · 언어~~ | **둘 다 정해짐** (위 §1) |
| 액티비티 운영 변경 정보 출처 | 2주차 — **서울권 표본 조사. 시작 가능** |
| `A2A`·`MCP`·`Docker`·`AWS` 를 어떻게 말할지 | `[미확보]` **v10 본문에 이 넷이 0회다.** 뺀 것인지 빠진 것인지 안 정해졌고 심사에서 물어볼 수 있다 |
| **49,000원** 수용 여부 · 인바운드 손익 재계산 `[정정 2026-09-10]` 59,000원(2도시)에서 내렸다 — v11 §0-4 결정 13 | **MVP 이후.** 미결정을 미결정으로 발표한다 |

## 8. 그 밖에 보고만 받으면 되는 것

| 항목 | 상태 |
|---|---|
| 유지 문서 대조 | **끝** — 18차까지, 잔여 0 |
| wiki 저장소 전환 | **끝 (2026-09-07)** → [D-012](../decisions/D-012-cutover-timing.md) |
| `docs/` 통합 | **끝 (2026-09-08)** — `wiki/records/` 로 들어갔다 |
| 결정 7건 (09-06 시트) | **전부 정해짐** |
| 강사 확인 둘 | **정리됨 (09-06)** — sLLM 파인튜닝은 계속 시도하는 트랙, 3W 산출물은 지금까지의 결과서와 체크포인트를 그대로 낸다 |
| DoD-01 원본 hash · 추적 영상 넷 | **닫힘 (09-06 확인)** |
| 네이버 주문 4건 누락 | **닫힘 (09-06 사용자 지시)** |

## 9. ★ 이전 판이 끝난 일을 시키고 있었다

`[실측 2026-09-09]` 이전 시트의 §2 는 **패치 6장을 적용하라**고 했다. 여섯 장 전부 `git apply --check` 를 통과했다는 표시가 붙어 있었다.

**지금 여섯 장 전부 적용에 실패한다.** 그런데 이유가 깨진 게 아니었다.

| 패치 | 지금 |
|---|---|
| 01 `research/index.md` 현재 기준 사실 | **이미 있다** |
| 02 쿠팡 확장 5.4.1 주의 | **이미 있다** |
| 03 참고판 README 제목 | **이미 있다** |
| 04 `data_go_kr` REPORT 취소선 | **이미 있다** |
| 05 DoD evidence 낡음 주석 9개 | **이미 있다** — 다른 문구로. 게다가 대상 경로 `final_project_cs/docs/` 자체가 2026-09-08 `wiki/records/` 통합으로 사라졌다 |
| 06 `.gitignore` sample wiki 예외 | **이미 있다** |

★**여섯 장 전부 이미 반영됐고 시트만 안 고쳐졌다.** 사용자가 시키는 대로 했으면 여섯 번 실패했을 것이다.

★**교훈 — 준비해 둔 작업물에는 유효기간이 있다.** `git apply --check` 통과는 **만든 시점의 사실**이지 실행 시점의 사실이 아니다. **시트를 낼 때가 아니라 사람이 실행하기 직전에 다시 재야 한다.**

`[실측]` 옛 패치는 `program/patches/2026-09-06/` 에 그대로 둔다 — **폐기가 아니라 반영 완료**이므로 지우지 않고 기록으로 남긴다.

## 관계

- [open-items.md](open-items.md) — 전체 열린 항목
- [../governance/domain-axis.md](../governance/domain-axis.md) — 문서 쪽 남은 일의 판정 기준
- [milestones/final-2026-10-26.md](milestones/final-2026-10-26.md) — 발표 전에 필요한 것
- [../../final_project_cs/wiki/teams/build-order.md](../../final_project_cs/wiki/teams/build-order.md) — 다음에 무엇을 만드나
