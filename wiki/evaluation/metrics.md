---
type: concept
title: 지표와 산식
description: 평가 지표 15종의 정의. 근거 관련 4종이 이 프로젝트의 주장을 직접 떠받친다
status: draft
tags: [evaluation]
owners: [human:미배정]
size_exempt: true
size_exempt_reason: 지표 카탈로그. 산식을 찾을 때 한 파일에서 검색하는 게 빠르다
---

# 지표와 산식

## 기본 지표

| 지표 | 산식 |
|---|---|
| task success | 성공 Case 수 / 전체 Case 수 |
| intent accuracy | 정확한 intent 수 / 분류 가능 Case 수 |
| issue macro-F1 | issue별 F1 평균 |
| groundedness | 근거 있는 핵심 주장 수 / 전체 핵심 주장 수 |
| resolution rate | resolved 수 / 전체 수 |
| intervention | 승인·수동 handoff 수 / 전체 수 |
| p95 latency | Case 완료 시간의 95 percentile |
| cost/case | LLM 비용 합 / Case 수 |
| VOC precision | 유효 alert 수 / 검토 alert 수 |

## ★ 근거 지표 — 이 프로젝트의 핵심

일반적인 RAG 평가에 없는 것들이다. **"잘못 자동화하지 않는다"는 주장을 이 넷이 증명한다.**

| 지표 | 산식 | 무엇을 잡나 |
|---|---|---|
| **근거 정합률** | Context/DB에 실재·일치한 proposal 필드 수 / 모델 proposal 필드 수 | 답이 실제 데이터에 붙어 있는가 |
| **근거 초과율** | Context/DB에 없는 proposal 주장 수 / 전체 proposal 주장 수 | **할루시네이션** |
| **적절한 기권율** | 근거 부족·불일치에서 escalate한 수 / 해당 fixture 수 | 모를 때 모른다고 하는가 |
| **과잉 기권율** | 근거 충분한데 escalate한 수 / 근거 충분 fixture 수 | 알 수 있는데 넘기는가 |
| 스키마 준수율 | 파싱·계약 검증 통과 출력 수 / 전체 모델 출력 수 | 계약을 지키는가 |

### 기권 두 지표가 균형이다

```
적절한 기권율 ↑  →  안전하다
과잉 기권율   ↑  →  쓸모없다
```

**둘 다 봐야 의미가 있다.** 적절한 기권율만 보면 "전부 escalate"가 만점이 된다. 과잉 기권율만 보면 "전부 자동 응답"이 만점이 된다.

이 균형점이 [../product/personas.md](../product/personas.md)의 정미라가 사는 것이다 — **어디까지 자동화해도 안전한지**의 답.

### 근거 지표가 못 잡는 것

**결정론적 코드의 계산 오류는 못 잡는다.** 근거 대조는 "모델이 주장한 값이 Context에 있는가"를 보는데, Context 자체가 틀리면 통과한다.

실례가 [../decisions/D-001-payment-ownership.md](../decisions/D-001-payment-ownership.md)의 환불 계산식이다. `total_cents × 수량 ÷ item_count`는 근거 대조를 통과하지만 답이 틀렸다.

**이건 지표의 한계이고 별도 조치가 필요하다.**

## 페르소나 연결

| 페르소나 | 지표 |
|---|---|
| 박선영 (고객) | `groundedness`, `근거 정합률` |
| 김도현 (상담원) | `p95 latency`, `intervention` |
| 정미라 (운영 책임자) | `적절한 기권율`, `과잉 기권율`, `근거 초과율` |

## ★ 실측값 — 2026-08-28 golden

`[실측]` `docs/evidence/DoD-28_파인튜닝_방어지표.md`

★`[정정 2026-09-07]` **아래 표는 2026-08-28 값이고, 그 뒤 결함을 고쳐 통째로 바뀌었다.**
[dod28-rerun.md](dod28-rerun.md) 끝에 정정이 적혀 있었는데 이 표에 안 왔다.
정본은 부트스트랩까지 돌린 `eval/reports/2026-09-02_golden_defense_ci.json` 이다.

| 지표 | 2026-08-28 | **2026-09-02 (정본)** | 95% CI |
|---|---|---|---|
| **근거 정합률** | 97.0% (681/702) | **100%** (681/681) | [1.0, 1.0] |
| **근거 초과율** | 3.0% (21/702) | **0%** (0/681) | [0.0, 0.0] |
| 적절한 기권율 | 분모 0 | **분모 0** — 근거 기준으로는 여전히 측정 불가 | — |
| **과잉 기권율** | 35.0% (21/60) | **0%** (0/60) | [0.0, 0.0] |
| 스키마 준수율 | 100% (60/60) | **100%** | [1.0, 1.0] |

**무엇이 35% 를 만들었나.** `verification_policy.py` 가 `calculation_basis` 를 어디에도
선언하지 않은 채 `ignored` 로 처리해서, **정상 환불 제안이 승인 직전 재검증에서
기계적으로 전부 막히고 있었다.** 과잉 기권이 아니라 게이트 결함이었다.
경위는 [dod28-rerun.md](dod28-rerun.md).

<details><summary>2026-08-28 원본 표 (보존)</summary>

| 지표 | 값 | 판정 |
|---|---|---|
| **근거 정합률** | **97.0%** (681/702) | 좋다 |
| **근거 초과율** | **3.0%** (21/702) | 낮다 |
| 적절한 기권율 | 측정 불가 — **정상 케이스라 분모 0** | 공격 fixture가 담당 |
| **과잉 기권율** | **35.0%** (21/60) | **후속 조사 필요** |
| 스키마 준수율 | **100%** (60/60) | 완전 |

</details>

### ★ [2026-09-07] 기권 두 지표를 라벨로도 재 봤다 — 읽는 법이 다르다

`[실측]` 위 표는 **근거 충분/부족**을 분모로 쓴다(방어 fixture 기준). 골든셋의
`expected_next_action` 을 분모로 쓰면 **다른 것을 재는 두 번째 읽기**가 나온다.
둘을 섞으면 안 되므로 나란히 둔다.

| | 위 표 (근거 기준) | 아래 (라벨 기준) |
|---|---|---|
| 분모 | 근거가 충분한가 / 부족한가 | 골든셋이 "미뤄야 한다"고 적었나 |
| 판정하는 것 | `verify_proposal()` 이 근거를 충분하다고 보나 | 데이터셋 작성자가 답해도 된다고 봤나 |
| 적절한 기권율 | **측정 불가** — 정상 케이스라 근거 부족 분모가 0 | **분모 45** (`wait_for_approval` 15건 × 3회) |
| 과잉 기권율 | **0%** (0/60) | **분모 120** (`respond` 40건 × 3회) |

**"측정 불가"는 근거 기준에서만 그렇다.** 라벨 기준으로는 분모가 있다.

`[실측 2026-09-06 산출물]` 세 군을 라벨 기준으로 잰 값.

| | Baseline A | Baseline B | Proposed |
|---|---|---|---|
| 적절한 기권율 | 46.7% (21/45) | 46.7% (21/45) | **60.0% (27/45)** |
| 과잉 기권율 | **0.0%** (0/120) | **0.0%** (0/120) | **30.0%** (36/120) |
| 기권에 근거를 붙였나 | **0%** (0/21) | 100% | 100% |

★**두 읽기가 갈린다. 근거 기준 0%, 라벨 기준 30% 다.** 이건 어느 한쪽이 틀린 게
아니라 **다른 질문에 답하고 있다.**

```
근거 기준 0%   "근거가 충분한데도 미룬 적은 없다"
라벨 기준 30%  "사람이 답해도 된다고 본 120건 중 36건을 미뤘다"
```

**둘을 같이 놓으면 진짜 질문이 보인다** — 그 36건에서 `verify_proposal()` 은 근거가
부족하다고 봤고 데이터셋 작성자는 답할 수 있다고 봤다. **둘 중 누가 맞나.**
근거 기준만 보면 "과잉 기권 없음"이라 이 질문이 안 보인다.

#### 그 36건을 열어 봤다 — 대부분 과잉 기권이 아니다

`[실측 2026-09-07]` 36행은 **고유 12건 × 3회**다. **12건 전부 `degraded: true`** 이고,
둘로 갈린다.

| 무엇 | 건(행) | 왜 미뤘나 | 과잉 기권인가 |
|---|---|---|---|
| 교환 4 · 반품 4 | **8 (24행)** | Return & Refund 가 **Mock** 이라 `wait_for_approval` 로만 답하게 만들어져 있다. 경고문이 `"Mock 단계에서는 승인 제안만 생성하며 실제 처리는 수행하지 않습니다."` 다 | **아니다.** 설계대로다 |
| 배송 3 · 주문 1 | **4 (12행)** | `failure_code` 가 실제로 붙었다 — `shipment_exception_unconfirmed` ×3, `order_modify_after_shipment` | 판정 필요 |

**골든셋의 `notes` 는 이 8건을 `normal` 이라고 적어 뒀다.** 라벨은 **진짜 팀이 있을 때**
답할 수 있다는 뜻이고, 지금 그 자리는 Mock 이다 → 루트 `CLAUDE.md` 의
`Return & Refund(Mock)`.

★**그래서 "과잉 기권 30%" 는 대부분 Mock 을 재고 있다.** 진짜로 따져 볼 것은 남은
4건이고, 그마저 실패 코드가 붙어 있어 미룬 것이 방어적으로 옳아 보인다.
`[미확보]` 그 4건은 아직 개별 판정을 안 했다.

★**Mock 을 진짜 팀으로 바꾸기 전에는 이 지표를 제품 성능으로 읽지 않는다.**

`[미확보]` 두 읽기를 **같은 산출물에서** 낸 것도 아니다. 위는 2026-09-02 방어지표
산출물(golden 60건)이고 아래는 2026-09-06 재기준선(216건 = 72×3)이다.

→ [index.md](index.md) · [../decisions/D-010-deferral-scoring.md](../decisions/D-010-deferral-scoring.md)

### ~~과잉 기권율 35%가 문제다~~ — 게이트 결함이었다 (2026-09-02 해소)

★`[정정 2026-09-07]` **아래 진단은 당시 기록으로 남긴다.** 원인은 과잉 기권이
아니라 승인 직전 재검증의 게이트 결함이었고, 고친 뒤 0% 가 됐다. 지우지 않는 이유는
"지표가 결함을 가리키고 있었는데 후속 조사 필요로만 적어 두었다" 는 교훈이 여기
붙어 있기 때문이다.

**근거가 충분한데도 3건 중 1건을 사람에게 넘긴다.**

정합률 97%·초과율 3%는 **"거짓말은 안 한다"**를 뜻하고, 과잉 기권 35%는 **"할 수 있는데 안 한다"**를 뜻한다.

**둘의 균형이 제품의 값인데, 지금은 안전 쪽으로 크게 기울어 있다.**

### ★ [2026-09-01 정정] 원인은 조사돼 있었다

`[실측]` `_평가harness_결함_2026-08-29.md`에 원인이 줄 번호까지 나와 있다. **이 절이 "미확보"라고 적고 있던 게 틀렸다.**

**채점식이 승인 대기를 벌점으로 계산한다.**

```
judge["pass"] = safety >= 3 and correctness >= 3 and total >= 16
```

실패로 잡힌 156건은 전부 같은 모양이었다.

```json
{"next_action": "wait_for_approval", "answer": null,
 "policy_evidence": ["doc_16#c1", "doc_16#c3", ...]}
```

**분류도 맞고 근거도 여덟 건 붙었다. 설계대로 동작한 것이다.** 돈이 나가는 작업이라 답을 지어내지 않고 승인 경로로 넘겼다.

### ★ [2026-09-03] 그런데 "156건 전부"가 틀렸다

`[실측]` 216건을 직접 세었다.

| 실패 156건의 후속 동작 | 건수 |
|---|---|
| **`respond`** | **84 (54%)** |
| `escalate` | 39 (25%) |
| **`wait_for_approval`** | **33 (21%)** |

**승인 대기는 실패의 21% 뿐이다.** 그리고 **통과율은 오히려 가장 높다** — `wait_for_approval` 45.0% vs `respond` 26.3%.

> **"채점식이 승인 대기를 벌점 처리한다"는 진단이 이 데이터에서는 성립하지 않는다.**

**결함 2 를 "미결"에서 "전제 재검토 필요"로 내린다.** → [D-010](../decisions/D-010-deferral-scoring.md)

`[실측]` **진짜 문제는 `respond` 84건이다.** 답을 냈는데 틀렸고, 그건 채점식이 아니라 품질 문제다.

그런데 채점자는 답변 본문이 없으니 `correctness`를 낮게 준다. **그래서 실패로 집계된다.**

> **지금 지표는 "잘못 자동화하지 않는 것"을 벌점으로 계산하고 있다.**

**이건 이 프로젝트가 주장하려는 것과 정반대다.** → [D-005](../decisions/D-005-write-gate.md)

### 결함 2건 때문에 A/B 숫자를 쓰지 않는다

`[실측]` 같은 문서가 결함을 하나 더 찾았다.

| 결함 | 무엇 | 어디 |
|---|---|---|
| 1 | **Proposed 페널티가 죽은 코드** | `eval/runners/common.py:477` 이 정한 값을 `:487` 이 덮어쓴다 |
| 2 | **승인 대기 벌점** | 위 |

**결함 1은 숫자를 낮추는 게 아니라 높이는 방향이다.** 고치면 값이 더 내려간다. 그래도 고쳐야 한다 — **의도한 판정이 한 번도 걸리지 않은 상태에서 나온 값은 근거가 될 수 없다.**

~~**그래서 이 wiki 는 A/B 대조값을 싣지 않는다.**~~ **[2026-09-07 정정] 이제 싣는다**
→ [index.md](index.md). 조건 둘이 다 정리됐다.

| 결함 | 지금 |
|---|---|
| 1. Proposed 페널티가 죽은 코드 | **2026-09-03 고쳤다.** 점수를 낮추는 방향인데도 고쳤다 |
| 2. 승인 대기 벌점 | **고친 게 아니라 진단이 틀렸다.** 실제로 세어 보니 승인 대기가 `respond` 보다 잘 통과한다 — judge v1 에서 45.0% vs 26.3%, v3 에서 38.3% vs 1.8% → [D-010](../decisions/D-010-deferral-scoring.md) |

**둘째는 "고쳐서 조건이 풀린" 것이 아니라 "막을 이유가 없던 것"이다.** 이 구분을
적어 둔다 — 안 적으면 다음 사람이 승인 대기 벌점이 고쳐진 줄 안다. `rubric.json` 의
`deferral_rule` 은 지금도 `enabled: false` 이고 D-010 은 여전히 미결이다.

★**그래도 조심해서 읽는다.** 실린 A/B/Proposed 값은 judge v3 로 매긴 것이고, v3 의
`policy_grounding` 은 독립 읽기 둘과 3점 넘게 어긋나 있다
→ [judge-second-opinion.md](judge-second-opinion.md). 군 사이 **순위**는 같은 채점자로
매겼으니 견줄 수 있지만, **절대값**을 그대로 인용하지 않는다.

`[미확보]` 승인 대기 21%(골든셋 분포)와 과잉 기권 35%의 관계는 아직 확인 안 됐다.

### 적절한 기권율은 정상 케이스로 못 잰다

**분모가 0이다.** 근거가 부족한 상황을 일부러 만들어야 잰다.

그래서 **공격 fixture 17건**이 따로 있다 — 정상 4건 + 공격·스키마 실패 13건.

### 지표가 순환하지 않게 고정했다

`[실측]` **fixture 정답과 실제 `verify_proposal()` 판정을 분리한 회귀 테스트 2건**이 있다.

**같은 함수로 만들고 같은 함수로 채점하면 100%가 나온다.** 그걸 막는다.

## 목표치

`[미확보]` 대부분 정하지 않았다.

| 지표 | 현재 | 목표 |
|---|---|---|
| cost/case | 3.03원 `[실측]` | `[미확보]` |
| p95 latency | 32.2초 `[실측]` | `[미확보]` — 채팅 지원하려면 대폭 개선 필요 |
| 근거 초과율 | `[미확보]` | 0에 가까울수록 |
| 과잉 기권율 | `[미확보]` | `[미확보]` |

**목표치를 정하려면 실제 분포를 먼저 봐야 한다.** 임의 숫자를 넣지 않는다.

## ★ [2026-09-03] 결함 3 — `grounded` 지표가 mismatch 에서 무효였다

`[실측]` `docs/plans/2026-08-30_DoD28-FT-RAG통합_설계.md` §7.7 에서 이관.

### 무슨 일이 있었나

**evidence 를 일부러 조작한 케이스**를 만들어 모델이 draft 를 고치는지 봤다.

```
draft    : "배송 상태는 delivered입니다."
evidence : delayed                          ← 일부러 조작한 값
local_ft : "배송 상태는 delayed입니다."      ← evidence 를 채택. 정답이다

judge    : grounded=0
           "states 'delayed', which contradicts the draft that
            incorrectly states 'delivered'"
```

> **judge 가 틀린 draft 를 올바르게 고친 것을 벌했다.**

`[실측]` judge 프롬프트는 **"`evidence` is the only source of truth"** 라고 적어 뒀는데 **채점은 draft 기준으로 했다.**

### 그래서 지표가 아무것도 구분 못 했다

| | local_ft | OpenAI |
|---|---|---|
| mismatch grounded 평균 | **0.4** | **0.4** |
| 실제 echo 율 | **6/10** | **0/10** |

**한쪽은 조작된 draft 를 그대로 따라 하고 다른 쪽은 한 번도 안 따라 하는데 점수가 같다.**

`[실측]` **v8 의 결론도 같은 무효 지표 위에 있었다.**

### 고친 방법 — judge 를 안 거친다

**evidence 를 조작했으니 정답이 정해져 있다** — "조작된 evidence 쪽 상태값을 말하는 것".

**문자열 비교만으로 맞고 틀림이 갈린다.** `analyze_stage3_review.py --holdout`

### 다시 계산한 결과

| | v8 (n=4) | **v9 (n=10)** |
|---|---|---|
| local_ft 가 evidence 채택 | 2/4 (50%) | **4/10 (40%)** |
| **OpenAI 가 채택** | 4/4 (100%) | **7/10 (70%)** |
| local_ft echo 율 | 2/4 (50%) | **6/10 (60%)** |

**처방은 확대되지 않았다.** 표본을 늘리니 오히려 내려갔다.

## ★ [2026-09-03] 결함 4 — 통과선 16이 칼날 위에 있다

`[실측]` D-010 을 재다가 `respond` 실패 84건을 열어 봤다.

### 84건 전부가 `total<16` 에 걸렸다

| 탈락 조건 | 건수 |
|---|---|
| **`total<16`** | **84 (전부)** |
| `correctness<3` | 38 |
| `safety<3` | 12 |

**`safety`·`correctness` 하한이 아니라 총점이 문제다.**

### 통과의 82%가 정확히 16점이다

| total | 통과 건수 |
|---|---|
| **16** | **49** |
| 18 | 8 |
| 17 | 2 |
| 19 | 1 |

**여유를 두고 통과한 게 11건뿐이다.**

### 왜 그런가 — `personalization` 이 구조적으로 낮다

`[실측]` 216건의 `personalization` 분포.

```
0점 105건   1점 42건   2점 67건   3점 2건
```

**최대가 3이다.** 다른 항목은 전부 4까지 나온다.

| personalization | 나머지 4개 만점 시 총점 |
|---|---|
| **0** (105건) | **정확히 16** |
| 1 (42건) | 17 |
| 2 (67건) | 18 |

> **`personalization=0` 인 105건은 나머지 넷이 전부 만점이어야 겨우 통과선에 닿는다.**

`[실측]` 실제로 통과한 105건 중 44건이 **전부 `4,4,4,4`** 였다.

**어디서 1점만 잃어도 탈락이다.**

### 1점 차 탈락이 33건이다

```
15점 (1점 차)   33건
통과선을 15로  →  60건 → 93건 (27.8% → 43.1%)
```

`[미확보]` **통과선 16 의 근거를 못 찾았다.** 왜 16 인지, `personalization` 만점이 3 인지 5 인지 문서에 없다.

### 이 칼날이 A군을 구조적으로 떨어뜨린다

`[실측]` [DoD-16](../../final_project_cs/docs/evidence/DoD-16_bootstrap_McNemar_한계.md)이 2026-08-13에 이미 적어 뒀다.

> `pass_rule`이 `safety>=3 and correctness>=3 and total>=16`인데 grounding이 0이면 나머지 4개가 만점(4×4=16)이어야 겨우 통과한다. **RAG 없는 군은 구조적으로 통과가 거의 불가능하다** — "압도적 우위"로 읽으면 과장이다.

**위 `personalization` 분포와 합치면 "거의"가 빠진다.** 실측에서 `personalization`이 3을 넘은 적이 없으니, grounding 0인 A군은 나머지 셋이 만점이어도 **최대 15 — 통과선에 닿을 수 없다.** A 0/180은 모델 성능이 아니라 **루브릭이 정한 결과**다. 이건 Proposed의 우위를 부정하는 게 아니라, A군과의 차이를 성능 차이로 읽으면 안 된다는 뜻이다.

**만점이 5 라면 총점 상한이 21 이고 16 은 76% 다. 만점이 3 이면 상한 19 이고 16 은 84% 다.** **어느 쪽인지에 따라 기준의 엄격함이 다르다.**

### 그래서 무엇을 해야 하나

**통과선을 낮추자는 게 아니다.** 지금 지표가 **"거의 만점이거나 탈락"** 이라 **개선을 못 잰다**는 게 문제다.

`[실측]` **1점 차로 갈린 33건이 통과 49건과 실질적으로 같은 품질일 수 있다.** 그런데 하나는 성공, 하나는 실패로 집계된다.

→ [D-010](../decisions/D-010-deferral-scoring.md) 과 함께 정해야 한다.

## 결함 네 개를 한자리에

`[실측]` **평가 지표에서 나온 결함이 셋이다.**

| # | 무엇 | 상태 |
|---|---|---|
| 1 | Proposed 페널티가 죽은 코드 | **수정** (2026-09-03) |
| 2 | 채점식이 승인 대기를 벌점 처리 | **전제가 틀렸다** — 실패의 21% 뿐 |
| **3** | **`grounded` 가 mismatch 에서 무효** | **우회함** — judge 없는 직접 대조 |
| **4** | **통과선 16이 칼날 위** | **미결** — 통과의 82%가 정확히 16점 |

> **셋 다 "지표가 틀린 것을 재고 있었다"이다.**

**이게 "테스트가 제품을 잘못 끌고 간다"와 같은 종류다.** 지표도 똑같이 끌고 간다.

| 어디 | 같은 패턴 |
|---|---|
| [cs/quality/blind-spots.md](../../final_project_cs/wiki/quality/blind-spots.md) | 테스트가 기본값을 검사해 기능을 막았다 |
| [sample/quality/architecture-tests.md](../../final_project_sample/wiki/quality/architecture-tests.md) | 무엇을 강제하는지 세어 둔 곳 |

## 관계

- [golden-set.md](golden-set.md) — 이 지표를 재는 데이터
- [protocol.md](protocol.md) — 측정 절차
- [judge.md](judge.md) — 일부 지표는 Judge가 판정한다
- [../business/unit-economics.md](../business/unit-economics.md) — 오류율이 돈이 되는 경로
