# 검증 로그 — 테스트 사각지대 회귀 테스트

- 실행일: 2026-08-31
- 계획: `docs/plans/2026-08-31_1154_테스트_사각지대_회귀테스트_설계.md`
- 커밋: 작업 시점 `9dae72d` 기준 (테스트만 추가)

## 1. 재현 명령

```
# 전체 테스트
cd final_project_cs
python -m pytest -q --no-header -p no:cacheprovider

# 결함 게이트 — 추가한 테스트가 실제로 그 변경을 잡는지
cd ..
python dojo.py defects --only <결함 id 목록>
```

## 2. 전체 테스트 (회귀 확인)

```
470 passed, 4 deselected, 36 warnings in 27.04s
```

추가 전 424 → 추가 후 470. 기존 테스트 실패 0건.

## 3. 결함 게이트 — 18건 전부 잡힘

각 줄은 "그 결함을 임시 사본에 적용하고 전체 테스트를 돌린 결과"다.
`✓` 는 **실패가 발생했다**(= 잡았다)는 뜻이다.

| 결함 | 결과 | 잡은 테스트 |
|---|---|---|
| INV-PII-001 | 2 failed | `test_email_keeps_only_the_first_letter` 외 |
| INV-PII-003 | 2 failed | `test_mask_json_descends_into_lists` 외 |
| INV-STATE-004 | 1 failed | `test_unregistered_event_is_rejected` |
| INV-IDEM-002 | 2 failed | `test_request_id_is_preferred_over_case_id` 외 |
| INV-CONFIG-001 | 2 failed | `test_undeclared_module_raises_instead_of_defaulting` 외 |
| INV-SCOPE-002 | 1 failed | `test_scope_guard_denies_a_principal_with_no_scopes` |
| INV-AUTH-001 | 1 failed | `test_any_seven_character_prefix_must_not_authenticate` |
| INV-RESPOND-001 | 1 failed | `test_respond_rejects_empty_answer` |
| INV-WAITINPUT-001 | 1 failed | `test_wait_for_input_rejects_other_wait_reason` |
| INV-HANDOFF-001 | 1 failed | `test_handoff_rejects_empty_capability` |
| INV-SCOPE-001 | 1 failed | `test_case_list_does_not_leak_across_customers_in_one_tenant` |
| INV-OUTBOX-001 | 2 failed | `test_publishing_a_case_under_another_tenant_is_rejected` 외 |
| INV-OUTBOX-002 | 1 failed | `test_publishing_without_any_tenant_is_rejected` |
| INV-CLASSIFY-001 | 2 failed | `test_classifier_rejects_one_invalid_label_at_a_time[intent…]` 외 |
| INV-COMMERCE-002 | 1 failed | `test_proposal_carries_the_evidence_it_was_built_from` |
| INV-COMMERCE-003 | 3 failed | `test_unknown_shipment_status_escalates_instead_of_answering[…]` |
| INV-COMMERCE-004 | 1 failed | `test_tracking_answer_counts_the_shipments_it_read` |
| INV-STATE-001 | 1 failed | `test_same_expected_version_has_one_success_and_one_state_conflict` (기존 테스트, **비결정적** — §4) |

게이트 기준선은 두 배치 모두 결함 없는 상태에서 전부 통과했다
(1차 `450 passed`, 2차 `470 passed`).

## 4. INV-STATE-001 단독 재현 — 결과가 갈리는 것을 확인했다

결함을 적용하고 그 테스트 **하나만** 5회 돌렸다.

```
결함 적용 + 단독 5회: ['passed', 'passed', 'passed', 'passed', 'FAILED']
```

4회 통과, 1회 실패. 전체 테스트 순서나 DB 상태와 무관하게 단독으로도 갈린다.
자세한 원인은 `docs/reports/debugs/2026-08-31_버전대조_가드_중복.md` 에 적었다.

## 5. 잘라낸 것

각 게이트 로그의 전체 출력은 길어 위 표로 요약했다. 원문은 실행 시점의
`defects --only` 출력이며 재현 명령으로 다시 만들 수 있다.
