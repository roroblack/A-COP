# 2026-08-17 16:01 — `scripts/seed_demo_cases.py` 재작성 검증 리포트

## 1. 작업 목표

`eval/runners/common.py`·`eval/datasets/*.jsonl` 마이그레이션(직전 리포트) 과정에서
발견한 것과 같은 종류의 잔여 결함 — `scripts/seed_demo_cases.py` 가 옛 구독·청구
도메인(`billing_subscription`/`technical_entitlement`, 지금 없는 team_id)을 그대로
쓰고 있어 발표 시연용 Case 생성이 현재 Team 구성과 맞지 않는 문제를 고쳤다.

## 2. 수행 내용

RULE.md §3.6 절차: `docs/handoff/_prompts/S-SEED-DEMO.md` 계약 작성 →
`codex exec -s workspace-write` 로 위임 → 산출물 독립 검수.

계약이 요구한 것:

- 상태 기계(EventType 시퀀스)는 그대로 유지, payload 값만 도메인 교체
- 하드코딩 UUID 금지 — `scripts/seed.py` 가 만든 실제 고객·주문·배송 행을
  `customer.external_id`+`order_no` 로 실행 시점에 조회
- 시나리오 1(배송완료 미수령)과 시나리오 2(교환 기한 문의)가 서로 다른 종단 상태
  (`waiting_approval` / `resolved`)를 보이도록 유지
- `scripts/seed.py` 자체의 docstring 오기 1건(`ORD-0103`→`ORD-0101`, 실제로는
  `ORD-0103` 이 `status='paid'` 라 배송이 생성되지 않아 시나리오에 못 씀) 수정

## 3. 검증 (Claude 가 독립적으로 재실행)

| 검사 | 명령 | 결과 |
|---|---|---|
| 소유 범위 준수 | `git status --short` | `scripts/seed.py`(1줄) · `scripts/seed_demo_cases.py` 만 변경 |
| `seed.py` 변경 범위 | `git diff -- scripts/seed.py` | docstring 한 줄만, 로직 무변경 확인 |
| 실제 실행 (Codex 자체 보고를 신뢰하지 않고 Claude 가 직접 실행) | `python -m scripts.seed && python -m scripts.seed_demo_cases` | 예외 없이 종료. `scenario_1: waiting_approval`, `scenario_2: resolved`, `cases_in_tenant: 2` |
| 재실행 안전성 | 같은 명령 2회 연속 실행 | `cases_in_tenant` 2 → 2 불변, 상태 동일 |
| 옛 도메인 어휘 잔존 | `grep -in "billing\|subscription\|entitlement\|payment_id" scripts/seed_demo_cases.py` | 0건 |
| 회귀 | `python -m pytest -q -m "not live"` | 294 passed (직전과 동일, 변화 없음) |
| 내용 정확성 | 코드 전문 읽음 | `doc_01#환불 금액의 산정`·`doc_15#교환 기한의 원칙` 이 실제 `_doc_index.json`/코퍼스에 존재. `action_type="refund.request"`·`capability="order.investigate"`/`"return.diagnose"` 가 실제 `TeamManifest` 값과 일치. 반품 이력이 있는 `cust_03`/`cust_04` 를 시나리오 2(resolved)에서 피하고 `cust_02` 를 쓴 것도 `ReturnExchangeTeam` 의 실제 분기 로직(`pending and order` 이면 무조건 승인대기)과 맞다 |

## 4. 산출물

```
scripts/seed.py                                          수정 (docstring 1줄)
scripts/seed_demo_cases.py                                전면 재작성
docs/handoff/_prompts/S-SEED-DEMO.md                       신규 (Codex 계약)
docs/reports/2026-08-17_S-SEED-DEMO_리포트.md               신규 (Codex 자체 보고)
docs/evidence/DoD-18_UI_시나리오_종단표시.md                  수정 (옛 도메인 값이라는 주석 추가, 브라우저 재확인 미수행 명시)
```

## 5. 미해결 이슈·다음 작업

- **브라우저로 화면을 다시 연 적은 없다.** DB 상태(`waiting_approval`/`resolved`)는
  실측했지만, `docs/evidence/DoD-18_UI_시나리오_종단표시.md` 의 "브라우저로 열어
  확인한 것"·"승인 버튼 클릭→종단" 절이 요구하는 수준의 재확인(uvicorn 기동 +
  `/ui/cases`·`/ui/approvals` 실제 열람 + 승인 클릭 관측)은 이번에 하지 않았다.
  필요하면:
  ```powershell
  python -m uvicorn app.presentation.api.app:app --port 8041
  # 브라우저로 /ui/cases, /ui/cases/<case_id>, /ui/approvals 열람
  # /ui/approvals 에서 시나리오1 승인 클릭 → waiting_approval(4)→resuming(5) 관측
  ```
- 이 작업으로 DoD 번호가 새로 닫히지는 않는다(DoD-18 은 이미 "통과" 판정이었고,
  이번 건은 그 판정의 근거 데이터가 최신 도메인과 어긋나 있던 것을 바로잡은
  것이다).
