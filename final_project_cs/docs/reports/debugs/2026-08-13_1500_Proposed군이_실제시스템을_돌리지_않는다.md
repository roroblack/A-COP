# 결함 — Proposed 군이 실제 시스템을 돌리지 않는다 (0/180)

- 발견: 2026-08-13 15:00, Claude 검수 (540런 완료 직후)
- 상태: **DoD 15·16 판정 보류.** 이 수치를 결과로 쓰지 않는다
- 위험도: **매우 높음** — 이 프로젝트의 중심 주장을 측정하는 군이다

## 1. 실측 (540행 전량, provider=openai, 비용 $0.385)

| 군 | 성공 | citations valid/invalid | grounding 평균 | personalization 평균 |
|---|---:|---|---:|---:|
| A (RAG 없음) | 0/180 | 0 / 8 (전부 무효) | 0.00 | — |
| B (RAG 있음) | **115/180** | **8 / 0** | **2.67** | 2.03 |
| **Proposed** | **0/180** | **0 / 0** | **0.00** | **0.21** |

★**Proposed 는 인용을 하나도 만들지 않는다**(claimed 자체가 0). A 처럼 지어낸 것도 아니고 **없다.**

Proposed 의 answer 표본:
> "구독과 결제 이력을 확인했습니다. 정책 근거를 바탕으로 안내할 수 있습니다."

**정형 스텁**이다. B 의 answer 와 비교:
> "It appears that you were charged for your Pro plan after cancellation.
>  We will investigate this issue and propose..."

## 2. 무엇이 잘못됐나

Proposed 러너가 **실제 시스템을 타지 않는다.**

- `ContextBroker` 가 만든 `ContextPack.evidence` 에는 policy 항목이
  이미 `doc_NN#cM` 형식 `source_id` 로 들어 있다(`app/core/context.py`).
  그것이 `policy_evidence` 로 **실리지 않는다**
- Team(`BillingSubscriptionTeam`/`TechnicalEntitlementTeam`)의 답변이 아니라
  **고정 문구**가 나온다
- `correctness 3.81` · `next_action 3.96` 이 높은 것은 intent/issue_code 를
  규칙으로 맞혔기 때문이고, **내용은 비어 있다**

## 3. 왜 이게 치명적인가

Proposed 는 **이 프로젝트가 만든 것 그 자체**다.
그것이 baseline B 보다 낮게 나오는 것은 결과가 아니라 **측정 실패**다.

이대로 리포트에 실었다면:
> "Context Broker + Agent Team 을 붙였더니 오히려 나빠졌다"

는 **정반대 결론**을 냈을 것이다.

## 4. 같은 종류가 반복되고 있다

이 프로젝트에서 네 번째다:

| 회차 | 증상 | 실제 원인 |
|---|---|---|
| 1 | route 5개 등록됨, 테스트 초록 | 인증 요청이 전부 500 (설정 우회) |
| 2 | `knowledge/ingest.py` 완성 | **적재를 안 돌림** (0건) |
| 3 | 평가 540행 생성 | provider=mock / 전 건 APIConnectionError |
| 4 | **Proposed 180행 생성** | **실제 시스템을 안 탐** |

★**"파이프라인이 돈다"와 "내용이 실린다"는 다른 사건이다.**
행 수·exit code·성공률만 보면 매번 통과처럼 보인다.

## 5. 고쳐야 할 것

1. Proposed 러너가 **실제 `ContextBroker` → `Team.execute()`** 경로를 타게 한다
2. `ContextPack.evidence` 중 `source_type='policy'` 인 항목의 `source_id` 를
   **그대로 `policy_evidence` 에** 싣는다 (이미 `doc_NN#cM` 형식이다)
3. `TeamResult.answer` 를 그대로 쓴다. 고정 문구를 만들지 마라
4. ★**검증**: Proposed 한 건을 돌려 `citations.valid` 가 **비어 있지 않고**
   `answer` 가 케이스마다 **다른지** 확인한다

## 6. 이번 실행 데이터 처리

540행 $0.385 는 **버리지 않고 보존**한다. A/B 결과는 유효하다:
- **A 0/180 · B 115/180** 은 인용 검증이 정상 작동한 결과다
- 다만 A=0 은 rubric 설계 영향도 있다 — grounding 0 이면 나머지 4개가 만점이어야 16점이다.
  "RAG 없는 군은 구조적으로 통과가 거의 불가능하다"는 **한계로 리포트에 적는다**

Proposed 만 다시 돌린다.
