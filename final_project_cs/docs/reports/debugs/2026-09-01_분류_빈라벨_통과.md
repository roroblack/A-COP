# 결함 리포트 — 분류 결과의 빈 라벨이 API 경로에서 통과한다

- 발견: 2026-09-01
- 상태: **코드는 고치지 않았다.** 회귀 테스트 작업의 범위가 "테스트만 추가" 였다.
- 발견 경위: `INV-CLASS-002` 회귀 테스트를 쓰다가 빈 문자열 케이스에서 걸렸다.

## 1. 위치

`app/presentation/api/cases.py:86`

```python
if not result or not all(k in result for k in ("intent", "issue_code", "sentiment")):
    raise ValueError("classifier unavailable")
```

`k in result` 는 **키가 있는지만** 본다. 값이 빈 문자열이거나 공백이어도 통과한다.

## 2. 재현

```
cd final_project_cs
python -m pytest "tests/integration/api/test_api_runtime.py::test_create_escalates_when_a_label_is_blank" -q
```

분류기가 `{"intent": "billing", "issue_code": "  ", "sentiment": "negative"}` 를
돌려주면 Case 는 `escalated` 가 아니라 `routing` 으로 간다.

## 3. 실측

| 입력 | 지금 결과 | 기대 |
|---|---|---|
| 키가 빠짐 (`issue_code` 없음) | escalated | escalated |
| 예외를 던짐 | escalated | escalated |
| **값이 공백 (`"  "`)** | **routing** | escalated |

앞 두 줄은 이번에 추가한 테스트가 지킨다. 세 번째 줄만 어긋난다.

## 4. 위험도

**낮음 — 지금 배선에서는 도달하기 어렵다.** 운영 경로의 분류기는
`app/modules/customer_ops/feedback.py::classify` 이고 거기서는
`not raw[key].strip()` 으로 공백을 거른다. 그래서 실제로는 예외가 나고 escalated 로 간다.

문제는 **두 곳의 기준이 다르다**는 것이다. `create_app(classifier=...)` 로 다른 분류기를
주입하면 API 쪽 가드가 유일한 방어인데, 그 가드가 값을 보지 않는다. 분류기를 교체하거나
provider 를 바꾸는 순간 빈 라벨이 Case 에 저장되고 그 라벨로 라우팅까지 간다.
`CLAUDE.md` §1 은 "값을 모르면 비워 둔다. 추정으로 채우면 그 오류가 조용히 고객 답변까지
간다" 고 적고 있다.

## 5. 제안

`app/presentation/api/cases.py` 의 조건을 값까지 보게 한다. 소유는 S-API 다.

```python
if not result or not all(
    isinstance(result.get(k), str) and result[k].strip()
    for k in ("intent", "issue_code", "sentiment")
):
```

고치면 `test_create_escalates_when_a_label_is_blank` 의 `xfail(strict=True)` 이
"예상과 달리 통과했다" 로 알려준다. 그때 xfail 표시를 떼면 된다.

## 6. 지금 한 것

테스트는 `xfail(strict=True)` 로 남겼다. 지우지 않은 이유는 두 가지다.
지우면 이 사실이 사라지고, 고쳤을 때 아무도 모른다.
