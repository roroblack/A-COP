# A-COP 6인 팀 init plan 작성 리포트

## 작업 목표

- 기준: `../A-COP_구현계획서_v6.md`
- 결과물: `docs/plans/2026-08-14_1546_A-COP_init_plan_6인팀_v1.md`
- 범위: v6 §16·§25와 부록 A를 6인 팀 실행 단위로 분해하고 저장소 현황과 pytest 실측을 반영

## 수행 내용

- `RULE.md`, `CLAUDE.md`, `docs/vision/ESTIMATION_BASELINE.md`, 기존 Codex 스트림 계획서를 읽었다.
- v6의 팀 배치, 10주 계획, 계약 경계, DoD, Alembic 규칙을 계획서에 반영했다.
- 1주차 계약 우선, fake 구현, Contract Freeze Day, Core 없이 가능한 병렬 작업을 명시했다.
- 코드 현황을 디스크에서 확인했다.
- 새 계획서만 추가했다. 기존 계획서와 v6는 수정하지 않았다.

## 실측과 검증

실측 명령:

```powershell
rg --files app/core/case_runtime app/core/access_action app/infrastructure/a2a app/infrastructure/graphstore app/infrastructure/graph_projection
rg -n 'class (TeamExecutorPort|GraphStorePort|MessageBrokerPort)|TeamExecutorPort|GraphStorePort|MessageBrokerPort' app/core app/infrastructure app/presentation
python -m pytest -q
```

결과:

- `app/core/case_runtime/`: 소스 0개
- `app/core/access_action/`: 소스 0개
- `app/infrastructure/a2a/`: `__init__.py` 1개
- `app/infrastructure/graphstore/`: `__init__.py`, `sql_adapter.py`
- `app/infrastructure/graph_projection/`: 소스 0개
- `TeamExecutorPort`: `app/core/remote_team/executor.py:8`
- `GraphStorePort`: `app/core/graph_retrieval/port.py:4`
- `MessageBrokerPort`: `app/core/contracts.py:334`, `app/infrastructure/messaging/ports.py:6`
- pytest: `163 passed, 3 failed, 1 deselected, 2 warnings in 32.22s`
- 실패 3건: `tests/integration/rag/test_rag_integration.py`가 OpenAI embedding을 호출할 때 `[WinError 10013]` 네트워크 차단으로 실패

## 미해결 이슈와 다음 작업

- 실제 계약을 `docs/handoff/`에 먼저 확정해야 한다.
- Core 1·Core 2 중첩 경로의 소스가 비어 있으므로 1주차 계약·fake 작업이 첫 구현 병목이다.
- `MessageBrokerPort` 중복 정의와 현재 Port 시그니처의 v6 정합화가 필요하다.
- RAG integration 3건은 네트워크 허용 또는 고정 embedding fixture가 있어야 재검증할 수 있다.

