# 2026-08-17 22:55 — DoD-18 브라우저 재검증, 승인 흐름 결함 2건 발견·수정

- 계획: 없음 (직전 세션에서 `docs/evidence/DoD-18_UI_시나리오_종단표시.md` 에 남긴
  "브라우저로 화면을 다시 연 적은 없다" 미해결 항목의 자발적 후속)
- 담당: Claude (전 과정 직접 수행)
- 수행: 데모 시나리오 1(환불 승인 대기)을 실 브라우저로 열어 승인 버튼을 눌러
  종단 흐름을 검증. 두 결함 발견·수정 —
  1. `verification_policy.py` `ignored` 에 `"evidence"` 누락 → UI 표시용 근거
     필드가 재검증(v7 §9-E)에서 "선언되지 않은 필드"로 거부되어 승인 자체가 409로 막힘
  2. `cases.py` 의 승인 감사 기록 행이 기본 `status="proposed"` 로 저장되어
     `/ui/approvals` 대기 큐에 유령 항목으로 영구히 남음 → `status=request.decision`
     으로 명시해 큐 조회에서 자동 배제되도록 수정
- 검증: 회귀 테스트 신규 2건(`tests/integration/api/test_approval_audit_row_excluded_from_queue.py`)
  추가, `pytest -q` 297 passed(기존 295+2), 실 브라우저로 승인 클릭→303→
  "대기 중 1, 근거 없어 잠김 0" 확인, DB 직접 조회로 `resuming` v5 전이 확인.
  데모 상태는 `python -m scripts.seed_demo_cases` 로 원복.
- 리포트: `docs/reports/2026-08-17_2255_DoD-18_브라우저재검증_결함2건_리포트.md`,
  `docs/reports/debugs/2026-08-17_2250_UI승인큐_유령항목.md`
