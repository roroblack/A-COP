# 2026-08-17 23:15 — Case 생성이 남기는 승인 큐 유령 항목 수정

- 계획: 없음 (직전 `action.approve` 유령 항목 결함 수정의 자발적 후속 —
  같은 뿌리(`create_action_request` 기본 status)가 다른 호출부에도 있는지 전수 조사)
- 담당: Claude (전 과정 직접 수행)
- 수행: `create_action_request` 호출부 4곳 전수 조사(`grep -rn` )로
  `app/presentation/api/cases.py:91`(`POST /v1/cases` idempotency 감사 기록)이
  같은 패턴의 결함임을 확인. `status="succeeded"` 명시로 수정.
- 검증: 신규 회귀 테스트 1건(`tests/integration/api/
  test_case_create_audit_row_excluded_from_queue.py`), `pytest -q` 298 passed
  (297+1). 실 서버에 실제 `POST /v1/cases` 호출해 DB 직접 조회로 확인,
  검증용 Case 는 FK 순서대로 정리해 데모 테넌트 원상복구.
- 리포트: `docs/reports/2026-08-17_2315_Case생성_승인큐_유령항목_수정_리포트.md`,
  `docs/reports/debugs/2026-08-17_2310_Case생성_승인큐_유령항목.md`
