# 접수 응답이 에이전트 실행 전체를 기다렸다 (2026-09-03)

## 해결됨 (2026-09-03 확인)

`cases.py` 가 `BackgroundTasks` 로 `_run_case_detached` 를 붙여 접수 응답 뒤에 에이전트를 돌린다. 접수는 더 이상 기다리지 않는다.

★**이 표기는 2026-09-03 에 붙였다.** 아래 `- 상태:` 줄은 **작성 시점**의
  상태이고 지금은 사실이 아니다 — `RULE.md` §4.1 이 발견 즉시 기록하게
  하지만 고친 뒤 되돌아와 닫는 단계가 없었기 때문이다
  (`wiki/records/reports/2026-09-03_결함리포트_상태를_읽을_수_없다.md`).
  당시 기록은 그대로 둔다.

---
v8 §3-A [2026-09-01 교정]이 인라인 분류 소유 문제와 함께 짚은 마지막 항목이다.

> `run_case` 호출처가 접수 라우트 하나뿐이라 접수 응답이 에이전트 실행 전체를
> 기다린다(실측 p50 20~34초, p95 32~51초).

## 무엇이 문제였나

`POST /v1/cases` 가 이 순서로 돌았다.

```
Case 생성 → 분류(LLM) → run_case(에이전트 전체 실행) → 응답
                          ~20~34초
```

접수는 "받았다" 를 답하는 일이다. 처리가 끝날 때까지 고객을 붙잡아 둘 이유가 없다.
게다가 이 구조에서는 에이전트가 느려지면 **접수 자체가 느려진다** — 문의를 받는
입구가 처리 속도에 묶인다.

## 고친 방향

`BackgroundTasks` 로 실행을 응답 뒤로 뗐다.

```
Case 생성 → 분류(LLM, 수 초) → 201 응답 → (뒤에서) run_case
```

★**분류는 접수 안에 남겼다.** LLM 한 번이라 수 초고, 그 결과가 있어야 응답이
`intent`·`issue_code`·`sentiment` 를 실어 보낼 수 있다. 느린 쪽은 에이전트
실행이었지 분류가 아니다.

## ★계약이 바뀐다

접수 응답에서 `run_id`·`next_action`·`resume_token` 이 **빠진다.** 응답은 이제
일곱 필드로 고정이다: `case_id`·`status`·`version`·`intent`·`issue_code`·
`sentiment`·`links`.

진행은 **`GET /v1/cases/{case_id}`** 로 확인한다.

★있다가 없다가 하는 필드를 두지 않은 이유: 실행이 아직 안 끝나 필드가 없는 것과
실행이 실패해 없는 것을 클라이언트가 **구분할 수 없다.** "없으면 실패" 로 읽히면
그게 곧 잘못된 안내가 된다.

기존 테스트 중 저 세 필드에 의존하던 것은 없었다(확인함). 계약 문서
`wiki/records/handoff/03_REST_MCP_인터페이스.md` §1-1 을 갱신했다.

## ★실패를 조용히 두지 않는다

떼어낸 실행은 아무도 안 본다. 그래서 `logger.exception()` 으로 스택까지 남긴다.
터져도 접수는 201 이고, Case 는 `routing` 에 남아 운영 화면에서 보인다.

## 남은 구멍 — 정직하게 적는다

`routing` 에 남은 Case 를 되잡는 sweeper 가 **아직 없다.** 실행이 중간에 죽으면
그 Case 는 아무도 다시 집지 않는다.

이건 같은 날 만든 `classifying` sweeper 와 **같은 성격**이다. 트랜잭션이나 요청
경계를 나눌 때마다 "그 사이에 죽으면?" 이 생기고, 그때마다 되잡는 장치가 필요하다.
`classifying` 쪽을 본으로 삼아 같은 모양으로 만들면 된다.

## 검증

테스트 2건 추가(`tests/integration/api/test_api_runtime.py`):

- 접수 응답에 run 필드가 없고 필드 집합이 정확히 일곱 개다
- 떼어낸 실행이 터져도 접수는 201 이고, 로그에 남는다

★`TestClient` 는 응답을 보낸 **뒤** background task 를 돌린다. 그래서 테스트에서
실행이 일어난 것은 확인되지만 "응답을 붙잡지 않았다" 는 것은 필드 집합으로
확인한다 — 시간으로 재는 테스트는 느리고 불안정하다.

전체 **591 passed · 4 deselected · 1 xfailed**.
