# 2026-08-12 S-WIRE

- `create_app()`의 기본 classifier를 `app.modules.customer_ops.feedback.classify`에 연결했다.
- 설정은 `get_settings()`만 사용하고, 기본 경로의 빈 API 키는 예외로 처리한다.
- fake classifier 주입 경로를 유지하고 API 성공/실패 통합 테스트를 추가했다.
- API 라우터를 팩토리 내부에서 생성해 앱별 classifier가 섞이지 않도록 했다.
- API 통합 테스트는 20건 통과했다.
- 전체 테스트는 106 passed, 3 failed이며, 실패 3건은 기존 RAG 테스트의 외부 OpenAI embeddings 네트워크 권한 오류다.
