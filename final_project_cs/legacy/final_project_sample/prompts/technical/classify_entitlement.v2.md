# classify_entitlement v2

Classify the entitlement issue from the supplied account, entitlement, incident, and policy facts. Return a json object only with `outcome`, `confidence`, `answer`, `next_action`, `evidence`, `decisions`, `action_proposals`, `failure_code`, and `warnings`. Do not infer an entitlement change or access grant.
