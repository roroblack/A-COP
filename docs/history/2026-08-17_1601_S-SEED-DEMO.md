# 2026-08-17 16:01 — `scripts/seed_demo_cases.py` 쇼핑몰 도메인 재작성

- 계획: (계획서 없음 — 직전 S-EVAL-DATASETS 작업 중 발견한 잔여 도메인 마이그레이션 결함)
- 담당: Claude(계약설계·검수) + Codex(S-SEED-DEMO, 스크립트 재작성)
- 수행: `scripts/seed_demo_cases.py` 가 옛 구독 도메인(`billing_subscription`/
  `technical_entitlement`, 지금 없는 team_id)을 쓰고 있던 것을 발견 → 계약 작성 →
  Codex 위임 → 시나리오1(배송완료 미수령, cust_01/ORD-0101)·시나리오2(교환 기한
  문의, cust_02/ORD-0201)로 재작성. `scripts/seed.py` docstring 오기 1건도 함께 수정.
- 검증: `python -m scripts.seed && python -m scripts.seed_demo_cases` 직접 실행 —
  `scenario_1: waiting_approval`, `scenario_2: resolved`, 재실행해도
  `cases_in_tenant` 불변. `pytest -q -m "not live"` 294 passed(변화 없음). 옛 도메인
  어휘 잔존 0건.
- 리포트: `docs/reports/2026-08-17_1601_S-SEED-DEMO_검증_리포트.md`
- 미해결: 브라우저로 `/ui/cases`·`/ui/approvals` 를 다시 열어 승인 클릭까지 확인하지
  않았다(`docs/evidence/DoD-18_UI_시나리오_종단표시.md` 에 명시).
