# 2026-08-17 23:50 — handoff 계약 문서 커머스 동기화, 프롬프트 감사추적 미배선 발견

- 계획: 없음 (앞선 승인 큐 유령 항목 결함 수정의 자발적 후속)
- 담당: Claude (전 과정 직접 수행)
- 수행: `docs/handoff/{03,05,06,07,08}.md` 의 옛 구독·청구 도메인 잔재(삭제된
  파일 경로·클래스명·scope 이름·golden 구성)를 실제 코드로 동기화.
  `04_Team_모듈_계약.md` 는 Team 예시를 실제 `OrderShippingTeam`/
  `ReturnExchangeTeam` 으로 교체하되, §3 "prompt 등록 규칙"이 기술하는
  `prompts`/`llm_calls` 감사추적이 **실제로는 배선돼 있지 않다**는 것을
  발견해 정직하게 "설계됐으나 미배선"으로 표시(고치지 않음 — 설계 결정
  필요). `08` 문서 §5/§6 은 반대로 "미구현"이라 적힌 구성기 하드코딩 문제가
  이미 해결돼 있음을 확인해 갱신.
- 검증: 문서 전용 변경, `pytest -q` 298 passed 로 회귀 없음 확인.
  `grep` 으로 남은 옛 도메인 잔재가 의도된 `10_도메인_교체_가이드.md` 뿐임을 확인.
- 리포트: `docs/reports/2026-08-17_2350_handoff계약_커머스동기화_리포트.md`,
  `docs/reports/debugs/2026-08-17_2340_프롬프트_감사추적_미연결.md`(핵심 발견, 미해결)
