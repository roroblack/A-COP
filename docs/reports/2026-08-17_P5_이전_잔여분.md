# P5 이전 잔여분 작업 보고서

## 변경 사항

- config/guardrails.yaml의 context.token_budget을 누락값과 구분해 표시하도록 판독기와 화면을 연결했다.
- SQLite 연결은 read-only로 유지하고, agent_runs 실행 이력과 customer_cases·outbox 상태 분포를 읽도록 했다.
- 실행 trace에 agent_runs, team_tasks, llm_calls, case_events 단계를 추가했다.
- 평가 run의 ablation 정보는 기존처럼 표시 대상으로 유지했다.

## 검증

    24 passed

기본 임시 디렉터리 권한 문제를 피하기 위해 임시 경로를 저장소 밖으로 지정해 검증했다.
