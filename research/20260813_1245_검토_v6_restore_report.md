# v6 §8·§9 복원 실측 보고

복원 대상은 `A-COP_구현계획서_v6.md`의 §8과 §9이다. 원본 `A-COP_구현계획서_A2A_Graph반영.md`의 해당 상위 절을 문자 그대로 복원했다. 원본 문서는 읽기만 했고 수정하지 않았다.

## §8·§9 문자 수

문자 수는 각 상위 절의 `## 8.` 또는 `## 9.`부터 다음 상위 절 직전까지를 Python `len()`으로 계산했다.

| 문서 상태 | §8 | §9 |
|---|---:|---:|
| A2A_Graph 원본 | 8,066 | 8,038 |
| v6 복원 전 | 2,295 | 1,327 |
| v6 복원 후 | 8,066 | 8,038 |

## 키워드 실측

아래 값은 문서 전체에서 Python `str.count()`로 센 횟수다. 복원 전 값은 작업 시작 시점의 v6를 기준으로 했다.

| 키워드 | 원본 | v6 복원 전 | v6 복원 후 |
|---|---:|---:|---:|
| `경합 종류` | 1 | 0 | 1 |
| `Coordination` | 14 | 5 | 14 |
| `Apache AGE` | 2 | 0 | 1 |
| `Signed Agent Card` | 1 | 0 | 1 |
| `위임 토폴로지` | 1 | 0 | 1 |
| `message_bus.publish` | 2 | 1 | 2 |
| `SharedStateUpdate` | 2 | 2 | 3 |
| `UpdateResult` | 2 | 2 | 3 |
| `판별` | 1 | 1 | 1 |

`Apache AGE`, `SharedStateUpdate`, `UpdateResult`의 복원 후 전체 문서 횟수는 기존 다른 절의 표현과 복원 절의 표현이 함께 반영된 값이다. §8·§9 범위에서는 원본과 복원 후가 동일하다.

## 검증 결과

- §8·§9 문자 수가 원본과 복원 후에 각각 일치한다.
- §8-C의 `경합 종류` 책임 표와 `SharedStateUpdate` / `UpdateResult` 계약을 확인했다.
- 9-C의 A2A 세 경로, 판별 기준표, Signed Agent Card, 개인 AI/MCP와 기업 Agent 플랫폼/A2A 구분, Task·Case 상태 매핑을 확인했다.
- 9-D의 Apache AGE, 위임 토폴로지, 설명가능성 경로, 판단 전환 조건을 확인했다.
- v6의 다른 절은 수정하지 않았다.
