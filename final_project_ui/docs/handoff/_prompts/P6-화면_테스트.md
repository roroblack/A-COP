# 구현 지시 — P6 화면 회귀 테스트

## 0. 작업 대상

★**`C:\Users\playdata2\Documents\final_workspace\final_project_ui` 안에서만 작업한다.**
다른 폴더는 **읽기만** 하고, 읽었으면 리포트에 적어라. **쓰지 마라.**

## 1. ★먼저 읽을 파일 (이것만)

이 저장소는 주석이 한글이다. **넓게 훑지 마라** — 파일을 많이 읽으면
컨텍스트가 깨진 글자로 차서 작업이 무산된다(실제로 두 번 그랬다).

```
console/web.py
console/db.py
console/readers.py
tests/test_connections.py     ← 이 파일의 스타일을 따라라
```

## 2. 왜 이걸 하나 — 결함이 통과했다

`web.py` 가 `state_card` 를 **만들어 놓고 body 에 안 넣고 있었다.**
섹션이 통째로 화면에서 사라졌는데 **테스트가 없어 아무도 몰랐다.**
코드는 있고 화면엔 없는 상태였다.

지금은 고쳤다. 그러나 **같은 실수가 다시 나면 또 못 잡는다.**

현재 **39 passed**. 화면 테스트는 **0건**이다.

## 3. 만들 것 — `tests/test_web.py`

`TestClient(create_app())` 로 실제 응답 본문을 검사한다.

### 3-1. ★약속한 섹션이 전부 그려지는가

```python
SECTIONS = ("조립", "무엇이 막혀 있나", "평가 실행", "상태 분포", "연결")
```

하나라도 빠지면 **어느 것이 빠졌는지 이름을 대며** 실패해야 한다.
`assert "조립" in body` 처럼 낱말만 찾지 마라 — 다른 곳에도 나온다. `<h2>…</h2>` 로 찾아라.

### 3-2. 값이 실제로 나오는가

- `guardrails` 의 `token_budget` 값(예: `12000`)이 본문에 있다
- 선언의 Team `team_id` 가 본문에 있다

### 3-3. ★없는 것을 0 으로 채우지 않는가

`CONSOLE_DATABASE_URL` 이 없을 때:
- 본문에 **`연결 안 함`** 이 있다
- 상태 분포 자리에 **`0` 을 지어내지 않는다**

### 3-4. 프로젝트가 아닌 경로

- "프로젝트가 아닙니다" 와 **왜 아닌지**(`config/project.yaml`)가 함께 나온다

### 3-5. 목록 화면

- 프로젝트가 아닌 폴더도 **판별 근거와 함께** 남는다 (조용히 빼지 않는다)
- ★**대상이 하나도 없어도 200 이고 화면이 뜬다** — 이 콘솔은 대상 없이도 떠야 한다

## 4. ★지킬 것

| 규칙 | 이유 |
|---|---|
| **fixture 는 `tmp_path` 로 만들어라** | 실제 형제 프로젝트를 읽으면 그 폴더 상태에 테스트가 묶인다 |
| **항상 참인 단언 금지** | 실제로 실패하는 상황을 만들어 검사해라 |
| **주석 문구를 검사하지 마라** | 과거 결함을 기록한 주석을 지우게 만드는 검사는 잘못된 검사다 |
| **대상 코드 복사 금지** | 이건 UI 만이다 |
| `pytest.skip` 금지 | |

## 5. 완료 조건 — ★출력으로 증명하라

```powershell
cd C:\Users\playdata2\Documents\final_workspace\final_project_ui
python -m pytest tests -q
```

★기대: **39 → 늘어난다.** 늘어난 숫자를 리포트에 **그대로 적어라**.
0 failed, skipped 0.

★그리고 **테스트가 실제로 실패할 수 있음을 보여라**:
`web.py` 의 `+ state_card` 를 잠시 지우고 테스트를 돌려 **빨간 것을 캡처**한 뒤
되돌려 놓아라. 그 출력을 리포트에 붙여라.
(항상 참인 단언이 세 번 나왔다 — 이번엔 실패하는 것을 눈으로 보여야 한다.)

## 6. 리포트

`docs/reports/2026-08-17_P6_화면_테스트.md`
— 테스트 증가 수, §5 의 **빨간 출력 원문**, 읽은 대상 폴더, 발견한 결함.

## 7. 하지 말 것
- ❌ 테스트 수가 그대로인 채 "완료"
- ❌ 항상 참인 단언
- ❌ 저장소 안에 임시 파일·로그 (`%TEMP%` 를 써라)
- ❌ `console/` 의 동작을 바꾸기 (이번엔 **테스트만** 추가한다)
- ❌ 실패하는 것을 보여주지 않고 "완료"
