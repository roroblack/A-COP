# P2 — 읽기 어댑터

- 실행: 2026-08-17
- 판정: 통과

## ★참조한 폴더 (허가받고 read-only)

형식 확인을 위해 **읽기만** 했다. 쓰지 않았다.

| 폴더 | 읽은 것 | 목적 |
|---|---|---|
| `final_project_sample` | `config/project.yaml` | 선언 구조 |
| | `docs/evidence/DoD-*.md` 의 **판정 줄만** | 판정 표기 변형 |
| | `eval/reports/*.jsonl` 첫 줄 3개 | 필드 이름 |
| `final_project_cs` | (탐지 실행 중 자동으로 읽힘) | 동작 확인 |

★**코드는 한 줄도 가져오지 않았다.** 형식만 보고 이 저장소의 파서를 따로 썼다.

## 재현 명령

```powershell
cd C:\Users\playdata2\Documents\final_workspace\final_project_ui
python -m pytest tests -q
```

## 실제 출력

```
........................                                                 [100%]
24 passed in 0.51s
```

실제 프로젝트 대상 실행:

```
탐지: [('final_project_cs', True), ('final_project_sample', True),
       ('final_project_ui', False), ('research', False), ('team_branch', False)]

=== final_project_cs ===
 선언: OK   modules: 6 / 7   teams: ['order_shipping', 'return_exchange']
 판정: {'통과': 24, '부분통과': 4}
 평가: 전체 23 표시 3
    rescored_proposed.jsonl | arm Proposed | provider openai | 실측$ 0.5215 | 추정$ 0.0819

=== final_project_sample ===
 선언: OK   modules: 7 / 8   teams: ['billing_subscription', 'technical_entitlement']
 판정: {'통과': 24, '부분통과': 4}
 평가: 전체 23 표시 3
```

★**경로를 주지 않았는데 형제 프로젝트 2개를 스스로 찾아 읽었다.** 목표한 동작이다.

## ★형식 확인에서 알아낸 것 (추측했으면 틀렸을 것들)

### 판정 표기가 6가지다

```
- 판정: **통과**
- 판정: 통과
- 판정: **통과 (기준선 기록)**
- 판정: **부분 통과** — 설명이 뒤에 붙는다
- 판정: 부분 통과
- 판정: 통과 (★한계는 아래 참조)
```

괄호·설명이 뒤에 붙고, `부분 통과` 에는 **공백**이 있다.
정규식이 앞 낱말만 잡고 공백을 정규화하지 않으면 `부분통과` 를 놓친다.

### 비용이 두 종류다

| 위치 | 무엇 |
|---|---|
| `config.estimated_cost_usd` | 돌리기 **전** 추정 |
| 행별 `cost_usd` 의 합 | **실측** |

실측 `$0.5215` vs 추정 `$0.0819` — **6배 차이**다.
하나로 뭉치면 거짓이 된다. 따로 낸다.

## 판정 근거

| 요구 | 결과 |
|---|---|
| 선언을 **plain data** 로 읽음 | **통과** — 대상의 검증 모델을 안 가져왔다 |
| ★깨진 선언도 **보고**함 | **통과** — 빈 dict 로 바꾸지 않고 `error` 를 채운다 |
| 판정 표기 6종 파싱 | **통과** — 실제 관측 형식으로 fixture |
| 재현 블록·실제 출력 유무 | **통과** — "통과라 적혀 있는데 근거 없는" 문서를 구분 |
| ★실측/추정 비용 분리 | **통과** |
| ★없는 값을 0 으로 안 채움 | **통과** — `None`(모름) |
| ★절삭을 말함 | **통과** — `total` · `shown` |
| mock 표시 | **통과** |

## ★포크가 아니다

이 저장소 전체가 **코드 210줄**이다 (`discovery.py` 105 + `readers.py` 105).
대상에서 가져온 것은 **0줄**이다.

- `ProjectConfig` 같은 검증 모델 없음 — 선언은 `yaml.safe_load` 로 dict 로만 읽는다
- 상태기계·Team·Controller·마이그레이션 없음
- 대상의 `theme.py` 도 안 가져왔다 (화면은 P3 에서 이 저장소가 만든다)

**대시보드는 선언이 유효한지 판정하지 않는다.** 그건 대상의 일이다.

## 한계

- **DB 를 아직 안 읽는다.** 실행 이력(`agent_runs` 등)은 접속 정보를 받는 구조가 필요하다
- **introspection HTTP 연결이 없다.** 대상이 떠 있을 때의 조립 실측은 P4
- `eval` 요약이 **첫 줄의 `config`** 를 대표로 쓴다. 한 파일에 여러 run 이 섞이면 틀린다 —
  지금 관측된 파일들은 파일당 run 하나였다
- 화면이 없다 (P3)
