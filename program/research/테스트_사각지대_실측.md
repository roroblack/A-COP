# final_project_cs 테스트 사각지대 (실측 자동 생성)

- 대상 revision: `git:9acd046`
- 기준선: 424 passed, 4 deselected, 36 warnings in 34.38s
- 방법: 불변식을 어기는 최소 변경을 임시 사본에 적용하고 전체 테스트를 돌렸다.
- 생성: `acop-dojo report` (원본은 `acop_dojo/acop_dojo/defects/catalog.json`)
- 원본 저장소는 건드리지 않았다. 사본에서만 적용하고 되돌렸다.

## 결론

불변식을 어기는 변경 31건을 넣어 봤다. 16건은 테스트가 잡았고 **15건은 424개가 전부 통과했다.**
잡히지 않은 것은 학습 문제로 쓸 수 없어 발견했고, 그 자체가 검증 공백이다.

## 잡히지 않은 것 — 테스트가 울지 않는다

| 불변식 | 바꾼 곳 | 무엇을 바꿨나 |
|---|---|---|
| 인증 헤더는 형식부터 검사한다 | `app/presentation/security.py` | Bearer 형식 검사가 사라졌다 |
| 분류가 실패하면 조용히 넘기지 않는다 — 잘못된 라벨은 거부한다 | `app/modules/customer_ops/feedback.py` | 분류 라벨 검사가 or 에서 and 로 바뀌었다 |
| project.yaml 에 선언되지 않은 모듈을 물으면 오류다 | `app/core/project_config.py` | 선언되지 않은 모듈이 켜진 것으로 취급된다 |
| handoff 는 handoff_capability 가 있어야 한다 | `app/core/contracts.py` | 넘길 곳 없는 인계가 통과한다 |
| 같은 요청을 두 번 실행해도 side effect 는 한 번만 난다 | `app/core/idempotency.py` | 멱등키의 요청 식별자가 Case 로 고정됐다 |
| 다른 tenant 의 Case 로 메시지를 발행할 수 없다 | `app/infrastructure/messaging/outbox.py` | outbox 발행에서 tenant 불일치 검사가 꺼졌다 |
| 값을 모르면 비워 둔다 — 추정으로 채우지 않는다 | `app/infrastructure/messaging/outbox.py` | tenant 를 모를 때 임시값으로 채운다 |
| PII 는 저장 시 마스킹하고 LLM 에는 masked 만 넘긴다 | `app/core/redaction.py` | 저장 전 마스킹에서 이메일 규칙이 빠졌다 |
| PII 는 저장 시 마스킹한다 — 구조 전체를 훑는다 | `app/core/redaction.py` | 마스킹이 리스트 안으로 내려가지 않는다 |
| respond 는 answer 가 있어야 한다 | `app/core/contracts.py` | 빈 답변으로 응답이 완료된다 |
| 모든 조회에 tenant_id 와 customer_id(또는 case_id) 조건을 적용한다 | `app/infrastructure/db/repository.py` | Case 목록이 customer 경계를 무시한다 |
| scope 없는 호출은 거부한다 | `app/presentation/security.py` | scope 가 하나도 없는 주체는 검사를 통과한다 |
| 낙관적 동시성 — 상태 변경은 읽은 시점의 version 과 정확히 맞아야 한다 | `app/core/transition.py` | 버전 대조가 헐거워져 지난 상태로 덮어쓴다 |
| payload schema 가 등록되지 않은 이벤트는 거부한다 | `app/domain/case.py` | 등록되지 않은 이벤트가 조용히 통과한다 |
| wait_for_input 은 wait_reason 이 customer_input 이어야 한다 | `app/core/contracts.py` | 입력 대기의 사유가 아무 값이나 통과한다 |

## 잡힌 것 — 대조군

| 바꾼 곳 | 깨지는 테스트 |
|---|---|
| `app/core/contracts.py` — 승인 대기인데 제안이 하나도 없어도 통과한다 | `test_wait_for_approval_needs_at_least_one_proposal` |
| `app/core/contracts.py` — ContextPack 토큰 예산 상한이 두 배로 늘어났다 | `test_context_pack_rejects_over_budget` |
| `app/core/contracts.py` — 축소했는데 무엇을 뺐는지 남기지 않아도 통과한다 | `test_degraded_without_omissions_is_rejected` |
| `app/core/contracts.py` — 사유 없는 에스컬레이션이 통과한다 | `test_escalate_requires_failure_code_or_warnings` |
| `app/core/contracts.py` — 근거 없는 확정 답변이 통과한다 | `test_answer_without_evidence_is_rejected` |
| `app/core/verification.py` — 근거 id 대조 방향이 뒤집혔다 | `test_evidence_outside_the_result_is_blocked` 외 6건 |
| `app/core/idempotency.py` — 멱등키에서 대상 필드가 빠졌다 | `test_idempotency_key_separates_business_subject_and_action_type` |
| `app/domain/case.py` — 상태 병합이 덮어쓰기가 됐다 | `test_state_patch_merges_and_does_not_wipe` |
| `app/core/redaction.py` — API 키 마스킹 규칙이 사라졌다 | `test_case_message_is_redacted_in_db_api_and_audit` |
| `app/core/redaction.py` — 마스킹이 잎 노드에서 아무 일도 하지 않는다 | `test_case_message_is_redacted_in_db_api_and_audit` |
| `app/core/verification.py` — 수량 대조가 값이 있을 때 건너뛴다 | `test_refund_over_the_order_total_is_rejected` 외 15건 |
| `app/domain/events.py` — 전이표에 없는 전이가 기본값으로 통과한다 | `test_stale_resume_on_resolved_case_is_handled_without_invalid_transition` 외 2건 |
| `app/domain/case.py` — 이벤트 payload 의 필수 키 검사가 사라졌다 | `test_classified_requires_all_three_labels` 외 1건 |
| `app/modules/customer_ops/return_refund.py` — 반품 가능 기간이 정책보다 길어졌다 | `test_expired_return_is_rejected_without_proposal` |
| `app/modules/customer_ops/response_review.py` — 답변 검토에서 사실 대조 결과를 버린다 | `test_refund_amount_above_order_total_is_fact_mismatch` 외 2건 |
| `app/core/verification.py` — 사실 조회에 실패해도 제안을 통과시킨다 | `test_failed_fact_lookup_rejects_everything` |

## 재현

```
cd acop_dojo
python -m acop_dojo defects --rebuild
python -m acop_dojo report
```

## 남기는 판단

여기서 테스트를 고치지 않았다. 담당 영역의 검증 설계에 관한 것이라 지시서로
넘기는 편이 맞다. 권한 계열(`app/presentation/security.py`)이 있으면 그쪽이 먼저다 —
권한 경로에 구멍이 나도 테스트가 울지 않는다는 뜻이기 때문이다.
