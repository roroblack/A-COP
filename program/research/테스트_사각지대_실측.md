# final_project_cs — 등록된 결함의 검출 상태 (실측 자동 생성)

- 대상 revision: `git:04f6634`
- 기준선: 486 passed, 4 deselected, 1 xfailed, 36 warnings in 63.16s (0:01:03)
- 방법: 불변식을 어기는 최소 변경을 임시 사본에 적용하고 전체 테스트를 돌렸다.
- 생성: `acop-dojo report` (원본은 `acop_dojo/acop_dojo/defects/catalog.json`)
- 원본 저장소는 건드리지 않았다. 사본에서만 적용하고 되돌렸다.

## 결론

불변식을 어기는 변경 49건 중 48건을 셌다(1건은 분모에서 뺐다 — 아래 참고). 48건은 테스트가 잡았고 **0건은 전체 테스트가 전부 통과했다.**

★**이 숫자는 저장소의 테스트 커버리지가 아니다.** 사람이 고른 48개 가설에 대한 검출률이다. 카탈로그에 없는 규칙은 여전히 보이지 않는다.
무엇을 말할 수 있고 무엇은 못 하는지는 문서 끝에 적었다.
**등록된 활성 결함 중 생존한 것이 없다.** 넣어 본 변경이 전부 어딘가에서 잡힌다.

이 상태를 유지하려면 새 규칙을 만들 때 그 규칙을 어기는 변경도 함께 만들어
게이트에 걸어 본다. 규칙만 늘리고 세는 곳을 안 만들면 다시 벌어진다.

## 분모에서 뺀 것

신호가 안정적이지 않아 세지 않는다. **뺀 것을 밝히지 않으면 0 이라는 수치가
무엇에 대한 0 인지 알 수 없다.**

- **INV-CLASS-002** — 잡을 수 없다. API 쪽 검사를 지워도 domain/case.py 의 validate_payload 가 CLASSIFIED 이벤트의 세 필드를 다시 검사해 InvalidTransition 을 던지고, cases.py 의 except 가 그것을 받아 classification_failed 로 전환한다. 즉 관찰 가능한 동작이 바뀌지 않는 중복 방어 제거다(subsumed mutant). 실제 결함은 값 검증 쪽이다 — final_project_cs/docs/reports/debugs/2026-09-01_분류_빈라벨_통과.md

## 잡힌 것 — 대조군

| 바꾼 곳 | 깨지는 테스트 |
|---|---|
| `app/core/contracts.py` — 승인 대기인데 제안이 하나도 없어도 통과한다 | `test_wait_for_approval_needs_at_least_one_proposal` |
| `app/presentation/security.py` — Bearer 형식 검사가 사라졌다 | `test_any_seven_character_prefix_must_not_authenticate` |
| `app/presentation/api/cases.py` — 승인 엔드포인트가 읽기 권한으로 열린다 | `test_approval_uses_rest_endpoint` 외 10건 |
| `app/core/contracts.py` — ContextPack 토큰 예산 상한이 두 배로 늘어났다 | `test_context_pack_rejects_over_budget` |
| `app/presentation/api/cases.py` — 분류 실패의 사유를 잃는다 | `test_create_escalates_when_injected_classifier_fails` |
| `app/modules/customer_ops/feedback.py` — 분류 라벨 검사가 or 에서 and 로 바뀌었다 | `test_classifier_rejects_one_invalid_label_at_a_time[intent-not_a_registered_intent]` 외 1건 |
| `app/modules/customer_ops/procurement_order_payment.py` — 제안이 근거 id 를 달지 않는다 | `test_proposal_carries_the_evidence_it_was_built_from` |
| `app/modules/customer_ops/fulfillment_logistics.py` — 배송 상태를 모르는데 아는 척 답한다 | `test_unknown_shipment_status_escalates_instead_of_answering[UNAVAILABLE]` 외 2건 |
| `app/modules/customer_ops/fulfillment_logistics.py` — 조회된 배송 건수를 세지 않고 0으로 답한다 | `test_tracking_answer_counts_the_shipments_it_read` |
| `app/modules/customer_ops/catalog_verification.py` — 계약에 없는 능력을 받아도 처리한다 | `test_compliance_check_always_escalates_without_claiming_a_result` 외 4건 |
| `app/core/project_config.py` — 선언되지 않은 모듈이 켜진 것으로 취급된다 | `test_require_module_on_undeclared_module_also_raises` 외 1건 |
| `app/core/contracts.py` — 축소했는데 무엇을 뺐는지 남기지 않아도 통과한다 | `test_degraded_without_omissions_is_rejected` |
| `app/core/contracts.py` — 사유 없는 에스컬레이션이 통과한다 | `test_escalate_requires_failure_code_or_warnings` |
| `app/core/contracts.py` — 근거 없는 확정 답변이 통과한다 | `test_answer_without_evidence_is_rejected` |
| `app/core/verification.py` — 근거 id 대조 방향이 뒤집혔다 | `test_evidence_outside_the_result_is_blocked` 외 6건 |
| `app/core/contracts.py` — 넘길 곳 없는 인계가 통과한다 | `test_handoff_rejects_empty_capability` |
| `app/core/idempotency.py` — 멱등키에서 대상 필드가 빠졌다 | `test_idempotency_key_separates_business_subject_and_action_type` |
| `app/core/idempotency.py` — 멱등키의 요청 식별자가 Case 로 고정됐다 | `test_request_id_is_preferred_over_case_id` 외 1건 |
| `app/presentation/api/mcp.py` — MCP 도구 하나가 쓰기 scope 를 요구한다 | `test_mcp_has_exactly_three_read_scoped_tools` |
| `app/domain/case.py` — 상태 병합이 덮어쓰기가 됐다 | `test_state_patch_merges_and_does_not_wipe` |
| `app/infrastructure/messaging/outbox.py` — outbox 발행에서 tenant 불일치 검사가 꺼졌다 | `test_no_row_is_written_when_the_guard_rejects` 외 1건 |
| `app/infrastructure/messaging/outbox.py` — tenant 를 모를 때 임시값으로 채운다 | `test_publishing_without_any_tenant_is_rejected` |
| `app/core/redaction.py` — 저장 전 마스킹에서 이메일 규칙이 빠졌다 | `test_email_keeps_only_the_first_letter` 외 1건 |
| `app/core/redaction.py` — API 키 마스킹 규칙이 사라졌다 | `test_case_message_is_redacted_in_db_api_and_audit` 외 2건 |
| `app/core/redaction.py` — 마스킹이 리스트 안으로 내려가지 않는다 | `test_mask_json_descends_into_lists` 외 1건 |
| `app/core/redaction.py` — 마스킹이 잎 노드에서 아무 일도 하지 않는다 | `test_case_message_is_redacted_in_db_api_and_audit` 외 2건 |
| `app/core/verification.py` — 수량 대조가 값이 있을 때 건너뛴다 | `test_refund_over_the_order_total_is_rejected` 외 15건 |
| `app/core/contracts.py` — 빈 답변으로 응답이 완료된다 | `test_respond_rejects_empty_answer` |
| `app/modules/customer_ops/response_review.py` — 금지어 검사가 사라졌다 | `test_forbidden_word_retries_then_passes` 외 3건 |
| `app/infrastructure/db/repository.py` — Case 목록이 customer 경계를 무시한다 | `test_case_list_does_not_leak_across_customers_in_one_tenant` |
| `app/presentation/security.py` — scope 가 하나도 없는 주체는 검사를 통과한다 | `test_scope_guard_denies_a_principal_with_no_scopes` |
| `app/core/transition.py` — 버전 대조가 헐거워져 지난 상태로 덮어쓴다 | `test_stale_write_on_an_advanced_case_is_a_conflict_not_a_transition_error` |
| `app/domain/events.py` — 전이표에 없는 전이가 기본값으로 통과한다 | `test_stale_resume_on_resolved_case_is_handled_without_invalid_transition` 외 2건 |
| `app/domain/case.py` — 이벤트 payload 의 필수 키 검사가 사라졌다 | `test_classified_requires_all_three_labels` 외 2건 |
| `app/domain/case.py` — 등록되지 않은 이벤트가 조용히 통과한다 | `test_unregistered_event_is_rejected` |
| `app/core/transition.py` — 이벤트를 역순으로 재생한다 | `test_replay_case_is_projection_equivalent` |
| `app/application/case_service.py` — checkpoint 가 업무 상태를 들고 다닌다 | `test_checkpoint_carries_no_business_state` 외 1건 |
| `app/core/transition.py` — SQL 쪽 버전 조건이 사라졌다 | `test_projection_update_keeps_the_version_condition` 외 1건 |
| `app/modules/customer_ops/return_refund.py` — 반품 가능 기간이 정책보다 길어졌다 | `test_expired_return_is_rejected_without_proposal` |
| `app/modules/customer_ops/response_review.py` — 답변 검토에서 사실 대조 결과를 버린다 | `test_refund_amount_above_order_total_is_fact_mismatch` 외 2건 |
| `app/core/contracts.py` — 승인 필요 제안을 첫 건만 보고 넘긴다 | `test_approval_required_proposal_anywhere_in_the_list_forces_wait` |
| `app/presentation/ui/routes.py` — 근거 없는 제안인데 결정 버튼이 살아 있다 | `test_evidence_free_proposal_has_disabled_decision` 외 1건 |
| `app/infrastructure/messaging/worker.py` — provider 실패를 성공으로 기록한다 | `test_timeout_is_unknown_and_not_automatically_retried[outbox_worker_contract]` 외 6건 |
| `app/core/verification.py` — 사실 조회에 실패해도 제안을 통과시킨다 | `test_failed_fact_lookup_rejects_everything` |
| `app/application/case_service.py` — checkpoint 의 graph_revision 이 하드코딩됐다 | `test_run_persists_fixed_graph_revision_and_checkpoint_is_not_projection_state` |
| `app/modules/customer_ops/feedback.py` — 분류 필수 필드 검사가 any 에서 all 로 바뀌었다 | `test_classifier_fails_when_any_field_is_missing[intent]` 외 3건 |
| `app/modules/customer_ops/voc_store_manager.py` — degraded 검사가 뒤집혔다 | `test_degraded_context_escalates_without_policy_lookup` 외 4건 |
| `app/core/contracts.py` — 입력 대기의 사유가 아무 값이나 통과한다 | `test_wait_for_input_rejects_other_wait_reason` |

## 원장 기준 — 규칙마다 세는 곳이 있는가

불변식 원장에 활성 규칙이 33개 있고 그중 33개에 결함이 붙어 있다.
위의 생존 수치는 **결함이 붙은 규칙에 한한 이야기다.** 결함조차 없는 규칙은
깨져도 게이트에 아예 안 잡힌다.

활성 규칙 전부에 결함이 하나 이상 붙어 있다.
## 이 숫자로 말할 수 있는 것

- 등록된 결함이 현재 환경의 전체 테스트에서 적어도 하나의 실패 신호를 낸다.
- 과거에 메운 사례가 다시 무방비로 돌아가는지 감시한다.
- 문서에 적힌 일부 불변식과 테스트 사이에 실행 가능한 추적 관계가 있다.

## 말할 수 없는 것

- 등록하지 않은 불변식이 테스트된다는 보장은 없다. **분모는 우리가 고른 것이다.**
- 모든 입력·상태 조합·실행 경로가 검증됐다는 뜻이 아니다.
- 결함을 잡은 테스트가 **올바른 이유로** 실패했다는 보장은 없다.
  무관한 예외나 fixture 실패도 잡은 것으로 집계된다.
- 카탈로그를 사람이 고르는 이상 선택 편향이 남는다. 경계 조건·동시성·상태 조합처럼
  patch 하나로 표현하기 어려운 위험은 분모에 잘 들어오지 않는다.

그래서 지표를 "사각지대 0" 이 아니라 **"등록된 활성 결함 생존 0건"** 으로 읽는다.

## 재현

```
cd acop_dojo
python -m acop_dojo defects --rebuild
python -m acop_dojo report
```

## 남기는 판단

여기서 테스트를 고치지 않았다. 담당 영역의 검증 설계에 관한 것이라 지시서로
넘기는 편이 맞다. 권한 계열(`app/presentation/security.py`)이 있으면 그쪽이 먼저다 —
권한 경로에 구멍이 나도 테스트가 울지 않는다는 뜻이기 때문이다.
