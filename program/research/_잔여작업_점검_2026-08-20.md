# 잔여작업 점검 2026-08-20

## 결론

지난밤 산출물에는 **명시적 모순 3건**이 있다. 근거 표본 10건은 정확 6건, 부정확 4건이다. `_정합성_수정제안.md`의 A 묶음 6건은 모두 미적용이다. DoD 실행 계획과 6개 Team 확충 설계는 계획 작성 뒤 착수된 증거가 없다.

| 요청 항목 | 결과 |
|---|---:|
| 발견한 모순 | 3건 |
| 근거 표본 검증 | 정확 6건 / 부정확 4건 |
| 아직 적용 안 된 안전한 제안 | 6건 |
| DoD 계획 착수 | 0건 |
| Team 확충 착수 | 0건 |

이번 감사의 우선순위 상위 5개는 다음과 같다.

1. DoD 6·16의 실제 파일 상태를 반영해 판정표와 합계를 다시 계산한다.
2. 부정확한 근거 4건과 Composer HEAD 오기를 바로잡는다.
3. A 묶음 6건을 대상 파일의 현재 내용에 맞춰 적용한다.
4. 번역 벤치마크 REPORT의 경로와 두 열의 라벨을 고친다.
5. DoD 15·16·28의 순환 선행 조건과 미확정 산출물 위치를 정리한다.

## 점검 범위와 방법

지정된 보고서 6개, 계획 문서 4개, 밤샘 요약, 다이어그램 근거 문서와 `.puml` 8개를 읽었다. `final_project_cs/`와 `final_project_sample/`은 근거 확인에만 사용했다. 테스트와 build는 실행하지 않았다.

근거 표본은 각 보고서의 백틱 안 `파일:줄` 인용을 중복 제거한 뒤 고정 seed `20260820`으로 5개씩 뽑았다. 따라서 같은 파일 상태에서는 표본을 재현할 수 있다.

## 문서 간 모순

### 1. DoD 6의 corpus와 ingest 검증

`_DoD_구현갭_분석.md:20,57`은 25문서와 300~400 chunk를 ingest하는 코드와 수량 검증 test가 없다고 적었다. `A-COP_DoD_잔여작업_계획.md:61-69`는 25개 문서, `knowledge/ingest.py`, corpus test가 이미 있다고 적었다.

실제 파일은 후자에 가깝다. `final_project_cs/knowledge/ingest.py:63-70,86-101`에 corpus 로드·수량 계산·ingest 경로가 있다. `final_project_cs/tests/integration/rag/test_rag_integration.py:9-29`는 25문서, 306 chunk, 1536차원 embedding을 검사한다. 같은 파일 45-49행은 tenant와 scope도 검사한다.

다만 테스트를 이번 감사에서 실행하지 않았으므로 통과 여부는 별도다. 정적 파일 존재를 판정 기준으로 삼은 기존 보고서의 DoD 6 판정은 다시 계산해야 한다.

### 2. DoD 16의 통계 구현

`_DoD_구현갭_분석.md:30,53`은 McNemar 구현, 통계 산식, bootstrap CI, 전용 test가 없다고 적었다. `A-COP_DoD_잔여작업_계획.md:107-117`은 `eval/stats/bootstrap.py`, `mcnemar.py`와 unit test가 이미 있다고 적었다.

실제 파일에는 paired bootstrap이 `final_project_cs/eval/stats/bootstrap.py:11-36`에 있다. McNemar exact·근사 계산은 `final_project_cs/eval/stats/mcnemar.py:11-52`에 있다. 두 통계 함수의 test와 60/20 dataset 수량 검사는 `final_project_cs/eval/tests/test_stats_and_datasets.py:8-46`에 있다.

실제 공백은 통계 함수 자체가 아니라 DoD 15 결과를 받아 단일 report와 CI gate로 잇는 경로다. DoD 16은 최소한 “전부 미구현”으로 둘 수 없으므로 판정과 전체 분포를 다시 계산해야 한다.

### 3. 고객용 Composer 포함 범위

`_밤샘작업_요약_2026-08-20.md:20`은 현재 `final-project-sample` 배포 설정이 두 패키지를 함께 포함한다고 적었다. `_컴포저_설계대비_구현대조.md:30`도 고객 build 제외 항목을 불일치로 판정했다. 반면 `A-COP_Composer_v3_불일치_해소안.md:17,26`은 `Dockerfile.customer`가 `acop_composer`를 복사하지 않는다고 적었다.

두 배포 경로를 분리해서 써야 한다. `final_project_sample/pyproject.toml:20-22`의 일반 package discovery에는 `acop_composer*`가 포함된다. `final_project_sample/Dockerfile.customer:8-15`의 고객 이미지는 `app/`과 `acop_basement/`만 복사하며 Composer를 제외한다. 일반 wheel 설정과 고객 이미지를 같은 “배포 설정”으로 묶은 표현이 모순의 원인이다.

## 근거 표본 검증

### DoD 구현 갭 분석

| 보고서의 인용 | 인용 문맥 | 결과 | 실제 확인 |
|---|---|---|---|
| `final_project_cs/app/core/context.py:252-274` | retrieval 실패·정책 누락을 degraded로 표시 | 정확 | 252-261행에서 두 조건을 판정하고 263-274행에서 `ContextPack`에 반영한다. |
| `final_project_cs/app/core/remote_team/executor.py:8-19` | Local executor가 `TeamResult` 계약으로 실행 | 정확 | Port와 Local adapter가 같은 async `execute`를 사용한다. |
| `final_project_cs/app/core/contracts.py:122-157` | `ContextPack` 12,000 예산과 초과 거부 | 정확 | 141행이 literal 12000이고 146-153행이 초과를 거부한다. |
| `final_project_cs/config/project.yaml:1-21` | 활성 Team은 `voc_store_manager` 하나 | 정확 | 18-21행에 활성 Team 한 항목만 있다. |
| `final_project_cs/tests/unit/test_project_composition.py:92-108` | `TeamExecutorPort` 교체 경계 검증 | 부정확 | 이 범위는 graph 비활성, 지원하지 않는 graph·broker port, 누락 config를 검사한다. Team executor 교체 성공을 검증하지 않는다. |

DoD 보고서는 **정확 4건 / 부정확 1건**이다.

### Composer 설계 대비 구현 대조

| 보고서의 인용 | 인용 문맥 | 결과 | 실제 확인 |
|---|---|---|---|
| `acop_basement/core/settings.py:50-52` | basement가 Composer JWT 설정을 보유 | 정확 | `composer_jwt_secret`, `composer_issuer_secret`이 해당 범위에 있다. |
| `acop_basement/presentation/api/app.py:17` | `create_app()`에 Composer router 인자가 있음 | 부정확 | 17행은 빈 줄이다. 시그니처는 현재 19행이다. |
| `acop_composer/service.py:108-116` | lock 안에서 revision 비교 후 충돌 발생 | 부정확 | 115행에서 lock은 시작하지만 비교는 현재 120-122행이다. |
| `acop_composer/api.py:48-57` | payload가 전체 `config`를 요구 | 정확 | 53-55행의 `CandidatePayload`가 `config`를 요구한다. |
| `acop_composer/api.py:90-98` | revision 충돌을 HTTP 409로 반환 | 부정확 | 90행은 빈 줄이고 92-98행은 apply 진입부다. 409 처리는 현재 100-104행이다. |

Composer 보고서는 **정확 2건 / 부정확 3건**이다. 두 보고서를 합치면 정확 6건, 부정확 4건이다.

줄 이동만 고치면 되는 세 건과 달리 `test_project_composition.py:92-108`은 근거 대상 자체가 다르다. 이 건은 올바른 Team executor 교체 test를 다시 찾아 연결해야 한다.

## 후속 작업의 적용 여부

### A 묶음

6건 모두 남아 있다.

| 제안 | 현재 확인 | 판정 |
|---|---|---|
| A-1 Team 카드 통합 | `A-COP_에이전트팀구성_브리핑.html:709`에 `Order & Payment`와 `Procurement`가 별도 카드로 남아 있음 | 미적용 |
| A-2 시장·출처 원본명 정리 | `program/research/index.md:49-50`에 두 원본명이 남아 있음 | 미적용 |
| A-3 버전 이력 원본명 정리 | `program/research/index.md:57-59`에 세 원본명이 남아 있음 | 미적용 |
| A-4 흡수 문서 행 삭제 | `program/research/index.md:61,63-65`와 34행의 역사 설명이 남아 있음 | 미적용 |
| A-5 존재하지 않는 설계 HTML 제거 | `A-COP_구현계획서_v8.md:407`에 두 HTML명이 남아 있음 | 미적용 |
| A-6 절 색인 보완 | `A-COP_구현계획서_v8.md:55-95`에 제안된 하위 절 행이 없음 | 미적용 |

안전한 미적용 제안 수는 **6건**이다. B-1과 C-1은 판단이 필요하므로 이 수에 넣지 않았다.

### DoD 잔여작업 계획

제안된 11개 항목의 착수 증거는 없다. 계획서가 새로 제안한 migration verifier, graph projector, scheduler, outbox replay test, consumer idempotency test, 통합 harness·report 파일은 모두 없다. 관련 계획 문서의 수정 시각보다 이 파일들의 기존 수정 시각이 앞선다.

DoD 6과 16의 기존 코드가 발견된 것은 계획 작성 뒤 착수한 결과가 아니다. 오히려 계획서가 기존 상태를 더 정확히 읽었고 갭 보고서가 이를 누락한 경우다.

### Team 확충 설계

확충 착수 증거는 없다. `final_project_cs/config/project.yaml:18-21`은 여전히 `voc_store_manager` 하나만 등록한다. `ResponseGenerationReviewTeam`은 `accepted_case_types=[]` 상태이며 미등록이다. 나머지 네 Team의 현행 module 구현도 확인되지 않는다.

따라서 계획서가 적은 기준 상태가 그대로다. 6개 Team 확충 중 계획 작성 뒤 착수된 것은 **0건**이다.

## 문서 자체의 결함

### 잘못된 경로와 식별자

`_번역모델_조사.md:179`의 `program/research/archive/2026-08-20/mt_bench_results/REPORT.md`는 존재하지 않는다. 실제 파일은 `datasets/mt/olist_reviews_mt_bench/REPORT.md`다. 이 REPORT 29행에는 지적된 잘못된 `원 리더보드 COMET-22`, `원 리더보드 BLEU` 라벨도 그대로 남아 있다.

`_컴포저_설계대비_구현대조.md:10`과 `_밤샘작업_요약_2026-08-20.md:78`의 HEAD `033eb2c`는 오기다. `final_project_sample`의 실제 HEAD는 `033ebc2`다.

### 계획 순서의 순환

`A-COP_DoD_잔여작업_계획.md:23-25`는 15 → 16 → 28 순서를 제안한다. 같은 문서 113행은 16 전에 28의 metric schema 합의가 필요하다고 적고, 173행은 28 전에 16의 report schema가 필요하다고 적는다. 16과 28의 선행 조건이 서로를 요구한다.

공통 metric·report schema를 별도 선행 작업으로 빼거나 한쪽이 소유하도록 바꿔야 한다. 현재 순서로는 착수 조건을 충족할 수 없다.

### 미확정 상태로 남은 설계

`A-COP_DoD_잔여작업_계획.md:123`은 CI 설정 위치와 Contract Freeze 산출물 위치를 실행 환경 결정 뒤로 미뤘다. 담당자, 확인 시점, 대체 evidence 형식이 없다.

`A-COP_예제Team모듈_확충설계.md:85`는 Action Gateway vocabulary 합의를 남겼다. 같은 문서 137행은 Remote Team의 `allowed_tools`를 두 선택지로 남겼고, 164행은 공통 뼈대를 상속 helper 또는 조합형 유틸 중 하나로 남겼다. 이 세 항목은 구현 전에 결정 목록으로 분리해야 한다.

이미 밤샘 요약의 결정 표에 올라간 Composer 방향, v2 소비자, 기준선 문서 범위는 여기서 중복하지 않았다.

### 문체 규칙

`_정합성점검_2026-08-19.md:17,31,45,56,71`의 부제 5개와 `_정합성_수정제안.md:13,73,83`의 부제 3개가 `— 설명` 형식을 쓴다.

`_DoD_구현갭_분석.md:5`는 한 문단에 볼드 4개를 쓴다. 표의 판정 셀과 Markdown 목록의 항목별 볼드는 문단으로 세지 않았다.

금지형 문장은 `_번역모델_조사.md:35,45,55,63-67,127,139,169,175,201-203`과 `A-COP_중간발표_현황정리.md:47,53,67,75,89,107,137,141,149`에 집중돼 있다. 사실 범위를 제한하는 의도는 유효하지만 `비교 조건`, `발표 가능 범위`, `미완료 조건`처럼 중립적인 판정문으로 바꾸면 훈계조를 줄일 수 있다.

## 다이어그램 점검

문제 없음. `_다이어그램_근거.md`의 유스케이스, 클래스, 시퀀스, 상태, ERD, 컴포넌트, A2A 시퀀스, 배포 설명은 실제 `.puml` 8개의 파일명과 title·주요 요소에 대응한다. `render_all.py`와 `build_showcase.py`도 같은 8종을 나열한다.

archive 안내도 실제 내용과 맞는다. `A-COP_UML_다이어그램_v2.html`에는 유스케이스·클래스·시퀀스 3종만 있다. `render_new.py`는 상태·ERD 2종을, `render_new2.py`는 컴포넌트·A2A·배포 3종을 렌더링한다. `acop_plan_basis_v2.txt`는 초기 근거·검증 보고서다.

다만 `_밤샘작업_요약_2026-08-20.md:72`는 배포도 가독성 문제를 조치했다고 적고, `A-COP_중간발표_현황정리.md:122`는 여전히 확대·부분 캡처가 필요하다고 적는다. 개선 후에도 큰 도면이라는 뜻으로 함께 해석할 수 있어 모순 건수에는 넣지 않았다.

## 다음 작업의 완료 기준

1. DoD 갭 보고서가 실제 corpus·통계 파일을 인용하고 29개 판정 합계가 다시 맞아야 한다.
2. 밤샘 요약과 중간발표 문서의 DoD 분포가 수정된 판정표와 같아야 한다.
3. 표본에서 부정확했던 네 근거가 현재 파일의 실제 줄과 주장에 맞아야 한다.
4. A 묶음 적용 뒤 삭제 대상 문자열을 재검색해 0건을 확인해야 한다.
5. 번역 REPORT 링크가 실제 경로를 열고 두 열이 XCOMET-XXL·COMETKiwi와 영→한 조건을 명시해야 한다.
6. DoD 16·28의 공통 schema 소유자와 선행 순서가 한 방향으로 정리돼야 한다.

이 기준을 충족한 뒤에 코드 잔여작업을 시작하면 지난밤 문서의 잘못된 기준선을 그대로 구현 계획에 전달하는 일을 피할 수 있다.
