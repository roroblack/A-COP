# jh 쉬운 설명 1차 검수

검수 대상: `team_branch/jh/A-COP_브리핑_v7_쉬운설명.html`

기준 문서: `A-COP_구현계획서_v7.md`, `program/research/20260816_0535_설계_A-COP_시스템지도.md`

## 수정 내역

| before | after |
|---|---|
| 자율형 AI 업무 운영 플랫폼 | AI 업무 운영 플랫폼. AI가 스스로 판단하되, 돈이 오가는 일은 사람 승인을 거친다 |
| 고객 상담 업무를 AI가 스스로 처리 | 고객 상담 업무를 승인 경계 안에서 AI가 처리 |
| 사람이 개입하지 않아도 될 만큼 자동화 | 커머스 업무로 확장하는 향후 vision이며 10주 구현 범위가 아님 |
| 평소엔 AI가 알아서 처리하고, 위험할 때 개입 | 매 건 결재하지 않고 평소에는 시스템이 처리하게 두었다가 이상할 때만 봄. human-on-the-loop와 human-in-the-loop의 차이도 명시 |
| 팀이 처리한 뒤 결과를 공유 게시판에 올림 | Team은 판단과 처리 방법을 제안하고, 실제 실행은 Controller가 승인 경로를 거침 |
| 팀 A/B/C를 그때그때 새로 만들어 등록 | 새 업무 팀은 프로그램을 뜯어고치지 않고 Registry에 명단만 등록 |
| 요청을 고객 문제 하나(Case) 형식으로 접수 | Case는 대화가 아니라 업무 단위이며, 승인 후 멈춘 지점부터 재개되는 기록 |
| Controller가 담당 팀을 정하고 일을 맡김 | Controller는 Capability와 담당을 정하고 Team은 제안을 내며, 실제 실행은 승인 경로에서 결정 |
| 내부·외부 팀이 일을 처리하고 결과를 받음 | Team은 제안을 내고 실제 Action은 Controller 승인 경로를 거치며, Team끼리는 직접 부르지 않음 |
| 처리 결과를 공유 게시판에 올리고 다시 판단 | Shared State에 결과를 남기고 Audit/Trace로 처리 이유를 처음부터 되감아 볼 수 있음 |
| MCP는 조회·가벼운 쓰기·결제를 3단계로 열어 줌 | MCP는 기본 읽기 전용이며, 되돌릴 수 있는 작업만 조건부 쓰기. 결제·환불·주문변경은 직접 실행 금지 |
| AI에게 거짓말하지 말라고 프롬프트로 부탁 | 없는 주문번호·틀린 금액은 프롬프트가 아니라 코드가 실제 데이터와 대조해 차단 |
| GraphStorePort는 연결 관계 조회 규격 | Port는 고정하고 SqlGraphAdapter는 교체 가능 |
| TeamExecutorPort는 팀에게 실제로 일을 실행시키는 통로 | Team의 판단 결과를 Controller가 실행 경로로 넘기는 통로 |
| Registry가 LOCAL/A2A를 정해 알려 줌 | 새 업무 팀은 프로그램을 뜯어고치지 않고 Registry 명단에 등록만 하면 됨 |

## 필수 개념 등장 횟수

대상 HTML 전체에서 단순 문자열 기준으로 집계했다.

| 개념 | 횟수 |
|---|---:|
| 할루시네이션 | 1 |
| 감사·재생 관련 표현 (`Audit`, `되감아`) | 4 / 1 |
| 도메인 팩 (`CS Pack`, `Commerce Ops Pack`) | 8 |
| `human-on-the-loop` | 4 |
| `human-in-the-loop` | 3 |
| Registry | 32 |

## 자체 검증

- 섹션 수: 25
- `</html>`로 정상 종료: 통과
- Python UTF-8 읽기 및 치환문자(`U+FFFD`) 검사: 통과
- Python HTML 파서 태그 스택: 미종료 태그 없음
- 수정 전후 `<style>` 블록: 이번 편집에서 수정하지 않음
- 쉬운 풀이 영역의 `자율`·`스스로`·`알아서`·`완전 자동`: 0건

## 고치지 못했거나 판단이 필요한 항목

- 원문(짙은 본문)에는 설계 용어로 `자율`이 남아 있다. 절대 규칙에 따라 원문은 수정하지 않았다. 위 0건 검사는 쉬운 풀이 영역 기준이다.
- 백업 `program/research/_backup/jh_쉬운설명_원본.html`은 현재 대상과 스타일 구조가 달라, 스타일 원본과의 독립적인 바이트 비교 기준으로는 사용할 수 없었다. 이번 패치에서는 `<style>` 블록을 편집하지 않았다.
- `Commerce Ops Pack`과 사이트 생성·판매는 10주 구현 범위가 아니라 vision이라는 구분을 쉬운 설명에 반영했다.
