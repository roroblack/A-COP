# 시스템지도 v7 갱신 리포트

갱신일: 2026-08-16  ·  대상: `program/research/20260816_0535_설계_A-COP_시스템지도.md`

## 추가·변경 항목

| 항목 | 반영 내용 | 기존 문서와의 차이 |
|---|---|---|
| Port 3종 | Port는 고정, Adapter는 모듈로 분리. `MessageBusPort`와 실제 `MessageBrokerPort` 차이를 명시 | 기존에는 `TeamExecutorPort`만 표기 |
| Commerce MCP | `cs.*` / `commerce.*` 두 표면과 해제 영향 추가 | 기존 MCP는 CS용 단일 표면 |
| 도메인 팩 | CS Pack(10주), Commerce Ops Pack(vision)을 모듈로, Team을 인스턴스로 표기 | 기존 팩 축 없음 |
| Response Generation & Review | side effect 없는 응답 생성·검증 모듈 추가 | 기존 항목 없음 |
| A2A Remote Team | `Catalog & Verification`, `execution_type: A2A` 추가 | 기존 A2A는 일반 executor 모듈만 표기 |
| 할루시네이션 방어층 | 근거 대조·실행 직전 재검증을 끌 수 없는 고정 경계로 추가 | 기존 항목 없음 |
| 배포 프로파일 | 로컬 / 단일 호스트 Docker / 고객사 자체 호스팅 추가 | 기존 항목 없음 |
| 인스턴스 축 | Team·Remote Agent·Tool·정책 문서·golden case 등 목록화 | 기존에는 Billing/Technical 2개만 기재 |
| 소유 경계 | 코어 2 / 모델 3 / 검증&프론트 1 배치에 맞춘 표 추가 | 기존 소유 표 없음 |
| 해제 영향 | 기존·추가 모듈 전체에 `끄면 무엇이 깨지나` 추가 | 일부 모듈만 표기되어 있었음 |

## 디스크 실측

| 대상 | 확인 결과 | 상태 |
|---|---|---|
| `TeamExecutorPort` | `app/core/remote_team/executor.py`; Local/A2A executor 존재 | 구현됨 |
| `GraphStorePort` | `app/core/graph_retrieval/port.py`; SQL adapter 존재 | 구현됨 |
| `MessageBusPort` | 정의 없음 | 없음 |
| `MessageBrokerPort` | `app/core/contracts.py`와 `app/infrastructure/messaging/ports.py` 중복 정의 | 골격만 |
| `app/infrastructure/a2a/` | `__init__.py`만 | 골격만 |
| `graphstore/` | `sql_adapter.py` 구현 | 구현됨 |
| `graph_projection/` | `__init__.py`만 | 골격만 |
| Commerce MCP | 설계만 있고 샘플 구현은 CS MCP 중심 | 없음 |
| A2A Remote fixture | executor/Agent Card PoC는 있으나 별도 remote 왕복 없음 | 골격만 |

## 테스트 실측

`final_project_sample/`에서 `python -m pytest -q`를 실행했다.

| 결과 | 수 |
|---|---:|
| 통과 | 196 |
| 실패 | 5 |
| deselected | 1 |

실패 원인은 승인 전 `guardrail_escalated` 전이 2건과 외부 OpenAI 임베딩 연결 차단 3건이다. 문서에는 이를 전체 구현 완료로 표시하지 않았다.

## 보존·수정 범위

시스템 지도는 기존 10개 항목을 남기고 확장했다. `A-COP_구현계획서_v7.md`, `A-COP_브리핑_v7.html`, `program/research/에이전트팀_구성.md`는 수정하지 않았다.
