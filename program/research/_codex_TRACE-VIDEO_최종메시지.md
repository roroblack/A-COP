작업을 진행할 수 없는 환경 권한 문제가 발생했다.

- 허용 경로 `program/onboarding/trace/**`에서 편집 도구가 기존 파일 수정과 새 파일 생성을 모두 거부했다.
- 실제 파일 변경은 없었다.
- 설치된 FFmpeg 9.0.1도 샌드박스가 실행 경로 접근을 차단했다. PyAV의 H.264 인코더는 사용 가능하다.

워크스페이스 쓰기 권한을 다시 활성화하거나 세션을 재시작한 뒤 요청을 이어 보내 주면, 분석한 구성대로 구현과 렌더링을 계속할 수 있다.