# 주중 작업 요약 2026-08-26 ~ 08-28

대상 기간은 8월 26일 수요일부터 28일 금요일까지 사흘이다. 작성 2026-08-31.
이어지는 주말은 [`_주말작업_요약_2026-08-29~30.md`](_주말작업_요약_2026-08-29~30.md)에 있다.

내용은 커밋 로그와 각 저장소의 리포트 파일에서만 가져왔다. 숫자는
`git log --numstat` 실측이다.

## 한눈에

사흘치지만 **작업은 사실상 28일 하루에 몰려 있다.**

| 날짜 | 루트 저장소 | `final_project_sample` | 무엇 |
|---|---:|---:|---|
| 08-26 수 | 0 | 0 | 이 저장소들에 기록이 없다 |
| 08-27 목 | 1 | 0 | 밤 11시 50분대, 확장 검토 문서 2건 |
| 08-28 금 | 12 | 3 | 이 사흘의 거의 전부 |
| 합계 | **13** | **3** | 파일 162회, +6,810 / -1,060 |

**8월 26일은 커밋도 파일 수정 흔적도 없다.** 저장소 밖에서 무엇을 했는지는
여기서 알 수 없으므로 비워 둔다. 지어내지 않는다.

재현 명령은 이렇다.

```bash
git log --since="2026-08-26 00:00" --until="2026-08-29 00:00" --pretty="%ad %h %s"
```

## 8월 27일 목요일

밤 11시 50분에서 52분 사이에 파일 셋이 바뀌었다. 전부 **확장 검토 문서**다.

`docs/vision/` 은 "지금은 안 한다"고 판정한 항목을 그 자리에서 적어 두는 곳이다.
나중에 무엇을 왜 미뤘는지 잊지 않기 위해서다. 이날 두 건이 늘었다.

- 고객 지식 수집과 승인형 RAG 색인
- 평가셋 작성 보조와 독립 검증

같이 `TODO_VISION.md` 를 갱신하고 스캐폴딩 CLI 문서를 손봤다.

## 8월 28일 금요일

### 1. 선언형 Team 실행기

**이게 뭔가.** 지금까지 새 Agent Team 을 하나 늘리려면 **파이썬을 새로 써서
배포**해야 했다. 이 실행기를 **한 번** 배포해 두면, 이후 새 Team 은 코드 없이
**선언만으로** 만든다. 이름, 역할, 프롬프트, 쓸 도구 목록을
`config/project.yaml` 에 적으면 그게 Team 이 된다.

**안전 경계를 셋 박았다.** 선언은 신뢰 경계 밖의 입력이기 때문이다.

- **읽기 전용이다.** 실행 제안을 만들지 않는다. 조회와 정리와 초안까지다.
  선언으로 결제나 환불 제안이 나오게 두면 프롬프트를 조작하는 것이 곧 업무를
  조작하는 것이 된다. 부수효과가 필요하면 코드로 만든 Team 을 써야 한다
- **도구 상한은 읽어들일 때 이미 걸린다.** 허용 목록이 읽기 전용 범위 밖이면
  선언을 읽는 단계에서 거부한다
- **근거가 없으면 답을 만들지 않는다.** 도구가 아무 사실도 못 주면 사람에게
  넘긴다. 조용히 일반 지식으로 메우지 않는다

**어디에 있나.** `final_project_sample/acop_basement/teams/declarative.py` 다.
테스트 두 벌(단위 163줄, 조립 82줄)이 함께 들어갔다.

**어떻게 작업했나.** 계획과 계약 문서를 먼저 등록하고(15:21) 그 다음 구현했다
(15:48). 계약을 먼저 고정하는 순서를 지켰다.

### 2. 카탈로그 기반 인스턴스 CRUD

**이게 뭔가.** 위 선언형 Team 을 **화면에서 만들 수 있게** 하는 쪽이다.
서버가 "이런 구현체를 꽂을 수 있다"는 목록을 알려 주면(`GET /composer/catalog`)
화면이 그중에서 골라 만들고 지운다(`POST /composer/changes`).

카탈로그에는 안정된 ID 만 노출하고 실제 파이썬 경로는 서버 안에만 둔다.
경로를 노출하면 화면이 제품의 내부 구조에 묶인다.

이 두 가지가 주말의 Composer 작업으로 이어진다.

### 3. Composer 화면 결함 3건 수정

운영 콘솔(`final_project_ui`)에서 실제로 눌러 보다 찾은 것들이다.

| 결함 | 증상 | 원인 |
|---|---|---|
| 적용 사유 누락 | 화면의 적용 버튼이 **항상** 422로 거부됐다 | 대상이 사유를 필수로 요구하는데 화면이 안 보냈다. 감사 기록의 근거라 필수다 |
| 계약 버전 기본값 | 실제 대상과 값이 안 맞았다 | 기본값이 `v1` 로 박혀 있었다. 실연결 확인 결과 `1.0` 이었다 |
| 토글 상태 형태 | 어떤 항목은 토글이 안 보였다 | 서버가 세 가지 모양으로 상태를 돌려주는데 화면이 한 가지만 알고 있었다 |

같은 날 v3 토글 어댑터와 화면도 추가했고, 인스턴스와 컴포넌트와 모듈을 구분하는
범례를 복원했다.

### 4. 산출물 문서 (하루에 세 번 다시 씀)

이날 산출물 문서를 **세 번** 고쳤다. 순서가 그대로 기록에 남아 있다.

| 시각 | 무엇 | 왜 |
|---|---|---|
| 16:14 | 기획서, WBS, 아키텍처, 요구사항 정의서, 화면설계서 작성 | 처음 채움 |
| 23:17 | 기획서를 **제출표 기준으로 다시** 작성 | 기획의 주된 목적과 내용이 공식 제출표와 달랐다 |
| 23:43 | 2주차 산출물 5종을 **도표와 함께 재작성** | 글만 있고 그림이 없었다. 근거 수치를 도표로 만들어 붙였다 |

두 번째 재작성이 중요하다. 처음 채운 기획서는 **우리가 생각한 주제**로 쓰여
있었는데, 공식 제출표에 적힌 주제와 범위가 달랐다. 제출표가 기준이므로 그쪽에
맞춰 다시 썼다.

이날 산출물 양식 폴더도 정리했다(14:46).

### 5. 스프린트와 에픽 설계

**이게 뭔가.** 공식 기간 8월 28일부터 10월 26일까지를 어떻게 끊고, 개발 일을
어떻게 묶을지 정하는 작업이다. 정본은
[`program/plan/A-COP_스프린트_에픽_설계.md`](../plan/A-COP_스프린트_에픽_설계.md)다.

스프린트 4개와 에픽 13건으로 정했다. 각 스프린트가 발표나 자연스러운 경계에서
끝나게 날짜를 맞춰서 길이가 19일, 13일, 16일, 12일로 제각각이다. 리뷰와 발표가
같은 날이면 준비를 두 번 하지 않는다.

**TeamFlow API 의 제약도 이날 기록했다.** 토큰으로는 이슈 생성만 되고 스프린트
생성은 막혀 있었다. 그래서 화면에서 사람이 실행할 수 있는 스크립트를 대안으로
만들어 뒀다. 실제 등록은 다음 날 권한을 켠 뒤에 했다.

### 6. 데이터셋 수치 정정

배송 이력 원본이 날짜별로 두 파일에 나뉘어 있었다. 합치면서 **같은 송장번호가
두 파일에 모두 있는 것**을 발견했다. 8월 21일에 다시 조회한 것이라 내용은 같고
조회 시각만 달랐다.

합계는 58줄인데 실제 송장은 57건이다. 중복을 제거하고 리포트의 수치를 정정했다.
**58과 57 중 어느 쪽이 맞는지 적지 않으면 나중에 세는 사람마다 다른 답을 낸다.**

### 7. 확장 검토 문서

확장 추천을 **자기 관측**과 **교차 프로젝트**로 나눠 등록했다. Composer v3
문서와 UI 원칙에도 정정을 반영했다.

## 이 사흘에서 이어진 것

28일에 만든 두 가지가 주말 작업의 재료가 됐다.

| 28일에 만든 것 | 주말에 무엇이 됐나 |
|---|---|
| 선언형 Team 실행기와 카탈로그 CRUD | Composer 카탈로그와 인스턴스 관리 화면 |
| 스프린트와 에픽 설계 | 다음 날 TeamFlow 실제 등록 |
| 산출물 5종 재작성 | 화면설계서 캡처 보강과 발표자료 19장 |

## 근거가 되는 커밋 목록

커밋 제목을 사람이 읽을 수 있게 줄인 것이다. 원본은 위 재현 명령으로 본다.

### 8월 27일 목요일

- 확장 검토에 예제 선택 생성 3층 등록과 전처리 자동화 두 항목 추가

### 8월 28일 금요일

- v3 토글 어댑터와 화면 추가, 인스턴스와 컴포넌트와 모듈 범례 복원
- 확장 추천을 자기 관측과 교차 프로젝트로 나눠 등록
- Composer v3 문서와 UI 원칙에 정정 반영
- 다른 세션의 데이터셋 작업 결과 반영
- 최종 산출물 문서 양식 정리
- 배송 이력 날짜별 원본 병합과 중복 1건 반영해 수치 정정
- Composer 결함 3건 수정
- 스프린트 4개와 에픽 13개 설계 등록
- 기획서, WBS, 아키텍처, 요구사항 정의서, 화면설계서 작성
- 스프린트 생성 스크립트 추가와 TeamFlow API 제약 기록
- 기획서를 제출표 기준으로 다시 작성
- 2주차 산출물 5종을 도표와 함께 재작성
- (sample) 선언형 Team 과 카탈로그 CRUD 계획·계약 등록
- (sample) 선언형 Team 범용 실행기
- (sample) 카탈로그 기반 인스턴스 CRUD
