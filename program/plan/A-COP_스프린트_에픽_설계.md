# A-COP 스프린트·에픽 설계

작성 2026-08-28. TeamFlow(https://jira-for-me.vercel.app)에 넣을 스프린트와 에픽의 정본이다.

## 한 줄 요약

공식 기간 2026-08-28~10-26을 **4개 스프린트 + 보완 구간**으로 끊고, 개발 작업을 **에픽 13개**로 나눈다.
산출물 21건은 에픽과 1:1이 아니다. 산출물은 제출물이고 에픽은 개발 단위다.

## 결론 — 왜 이렇게 끊었나

Codex와 상의했다. Codex 권고는 **2주 단위 4개 개발 스프린트 + 기간 표기 없는 9W는 별도 보완 구간**이었다.
근거는 셋이다. 산출물 마감일을 스프린트 종료일로 억지로 옮기지 않는다. 10/21은 7W와 8W가 하루 겹치므로
개발 용량을 한 번만 계산한다. 9W는 개발 주차가 아니라 산출물 보완 구간이다.

여기에 한 가지를 조정했다. **각 스프린트가 발표나 자연스러운 경계에서 끝나게 날짜를 옮겼다.**
스프린트 리뷰와 발표가 같은 날이면 준비를 두 번 하지 않는다. 그래서 길이는 정확히 2주가 아니라
19·15·14·12일이 됐다.

| 스프린트 | 기간 | 일수 | 끝나는 지점 | 부트캠프 주차 |
|---|---|---:|---|---|
| S1 기반 계약 고정 | 2026-08-28 ~ 09-15 | 19 | **중간발표 09-15** | 1W~3W |
| S2 도메인 팀 구현 | 2026-09-16 ~ 09-30 | 15 | 추석 구간 종료 | 4W |
| S3 평가와 통합 | 2026-10-01 ~ 10-14 | 14 | 5W·6W 산출물 마감 | 5W~6W |
| S4 배포와 시연 | 2026-10-15 ~ 10-26 | 12 | **최종발표 10-26** | 7W~9W |

선행 구간(2026-08-17~08-27, 11일)은 스프린트로 만들지 않는다. 이미 끝났고, 그 결과물은 S1의
시작 상태다. TeamFlow에는 넣지 않고 이 문서에만 기록한다.

### 안 A(부트캠프 주차 그대로 9개)를 안 고른 이유

산출물 마감과 1:1로 맞는 점은 좋다. 그러나 4W가 11일, 8W가 4일이고 8W는 7W와 10/21 하루가 겹친다.
길이가 4일에서 11일까지 흔들리면 번다운 차트의 기울기를 스프린트끼리 비교할 수 없다.
겹치는 하루는 두 스프린트에 같은 사람의 시간을 두 번 계산하게 만든다.

### 안 C(수행단계별 6개)를 안 고른 이유

단계 구분과는 맞지만 길이가 더 제각각이다. 기획은 1주인데 모델링은 3주가 넘는다.
그리고 실제로는 단계가 겹쳐서 돈다. 데이터 전처리를 하는 동안 Core 구현이 같이 굴러간다.
단계를 스프린트로 만들면 이 겹침을 표현할 수 없다.

## 지금 이미 된 것

에픽 상태를 매기기 전에 현재 상태를 확인했다. 근거는 셋이다.

| 확인 대상 | 결과 | 확인 방법 |
|---|---|---|
| DoD 29건 판정 | 통과 26 · 부분통과 3 · 판정 없음 1 | 2026-08-28 개발 콘솔로 `final_project_cs/docs/evidence/` 읽음 |
| 부분통과 3건 | DoD-15(AB Proposed 60x3 holdout), DoD-17(마일스톤 게이트), DoD-28(파인튜닝 방어지표) | 동 |
| 판정 없음 1건 | DoD-EVAL-DATASETS | 동 |
| 선언형 Team 실행기 | **구현됨** (`acop_basement/teams/`) | `final_project_sample/docs/reports/2026-08-28_S-DECLARATIVE-TEAM-RUNTIME_리포트.md` |
| Composer 카탈로그·CRUD | **구현됨** (`GET /composer/catalog`, `POST /composer/changes`) | `final_project_sample/docs/reports/2026-08-28_S-COMPOSER-CATALOG-CHANGES_리포트.md` |

★뒤의 두 건은 오늘 다른 세션이 `final_project_sample`에 넣었고 **아직 커밋되지 않았다.**
`final_project_cs`에는 아직 없다. sample에서 만들어 cs와 UI가 가져다 쓰는 관계이므로
(`A-COP_Composer_소유권_정정.md`), cs 이식과 UI 연결이 남은 작업이다.

## 에픽 13개

담당은 `A-COP_구현계획서_v8.md` §16의 역할이다. 사람 이름이 아니라 역할로 적는다.

| 에픽 ID | 이름 | 담당 | 목표 스프린트 | 연결 산출물 | 완료 판정 |
|---|---|---|---|---|---|
| E01 | 케이스 생명주기와 상태 전이 | 코어 1 | S1 | 요구사항 정의서 | DoD-03 append-only replay |
| E02 | Context Broker와 ContextPack | 코어 1 | S1 | AI 시스템 아키텍처 | DoD-22 Team 직접 Tool 호출 금지 |
| E03 | Team Registry와 라우팅 | 코어 1 | S1 | AI 시스템 아키텍처 | 조건 하나에 팀 하나. no_team은 escalated |
| E04 | Shared State·CAS와 Outbox 전달 보장 | 코어 1 | S1 | DB·저장소 설계 문서 | DoD-12 outbox 원자성 replay |
| E05 | Gateway와 REST·MCP·A2A 진입 | 코어 2 | S1~S2 | 요구사항 정의서 | DoD-14 API키 scope 구분 |
| E06 | Action 승인·멱등성·감사 | 코어 2 | S1~S2 | 서비스 테스트 보고서 | DoD-11 동일 요청 10회 부수효과 1회 |
| E07 | VOC & Store Manager 팀 | 모델 | S2 | 멀티 에이전트 테스트 보고서 | 급증 탐지와 위임 판단 골든셋 |
| E08 | Response Generation & Review 팀 | 모델 | S2 | 멀티 에이전트 테스트 보고서 | DoD-29 응답 검증 |
| E09 | Commerce Ops 팩 4종 | 모델 | S2~S3 | 멀티 에이전트 테스트 보고서 | Return&Refund는 Mock, Catalog는 A2A Remote |
| E10 | 데이터 수집·전처리와 RAG 색인 | 모델·공통 | S1~S2 | 수집 데이터 보고서, 전처리 결과서 | VOC 5종 전처리 완료, pgvector 색인 |
| E11 | 평가 harness와 통계 | 검증·프론트 | S3 | ML/DL 학습결과서, 벡터DB 구축 결과서 | golden 60 + holdout 20, 부트스트랩 95% CI |
| E12 | 운영 콘솔과 Composer 구성 관리 | 검증·프론트 | S3~S4 | 화면설계서, LLM 연동 웹 애플리케이션 | 카탈로그 기반 인스턴스 CRUD가 UI에서 동작 |
| E13 | 산출물 문서와 발표 | 전체 | S1·S4 | 21건 전체 | 중간발표 09-15, 최종발표 10-26 |

에픽을 13개로 묶은 이유는 6명이 9주에 다룰 수 있는 크기여야 하기 때문이다.
DoD 29건을 그대로 에픽으로 만들면 담당자당 5개씩 흩어져 진행률이 안 보인다.

## 의존성

앞의 것이 안 끝나면 뒤의 것을 만들어도 동작하지 않는다.

| 선행 | 후행 | 근거 |
|---|---|---|
| E01·E02·E03 계약과 stub 고정 | E05~E09 전부 | 계획서 §18-A — 1주차에 계약을 고정해야 코어 1 병목이 안 생긴다 |
| E02 Context Broker | E07·E08·E09 | 팀은 조회 도구를 직접 부르지 않는다. ContextPack이 없으면 팀이 돌 근거가 없다 |
| E06 승인·멱등성 | E09 Commerce Ops | 발주·환불은 부수효과다. 승인 경계 없이 열 수 없다 |
| E10 데이터·색인 | E11 평가 | 골든셋을 만들려면 전처리된 데이터가 먼저 있어야 한다 |
| 선언형 Team 실행기 (완료) | E12 Composer 인스턴스 CRUD | `A-COP_남은작업_인수인계.md` — 실행기 없이는 카탈로그에서 고를 것이 없다 |
| E11 평가 결과 | GraphRAG 채택 판단 | 계획서 §9-D — 8~9주차 비교 실험 게이트를 통과할 때만 확장한다 |

## 확인하지 못한 것

- TeamFlow 프로젝트가 `SKN32_FINAL_TEAM4`인데 부트캠프 시트의 우리 팀은 **6팀**이다.
  이름이 다른 이유를 확인하지 못했다. 등록 전에 사람이 확인해야 한다.
- 스토리 포인트를 배정하지 않았다. 팀이 함께 추정해야 하는 값이고, 혼자 정하면 의미가 없다.
- 에픽별 사람 이름을 넣지 않았다. §16은 역할 배분이고, 6명 중 누가 어느 역할인지는 확정된 기록을 못 찾았다.
- sLLM 파인튜닝은 6팀 필수 여부가 불명확해(시트 문구가 "3, 4번 주제") 별도 에픽으로 두지 않고
  E11 안의 선택 작업으로 남겼다.
- 각 에픽의 세부 스토리는 나누지 않았다. 담당자가 스프린트 계획 회의에서 쪼개는 것이 맞다.

## TeamFlow 등록 상태

2026-08-28 시점, 발급받은 API 토큰으로는 등록할 수 없다.

| 시도 | 결과 |
|---|---|
| `GET /api/projects` | 성공. 프로젝트 3개 조회됨 |
| `GET /api/issues?projectId=84` | 401 — 조회는 토큰이 아니라 로그인 세션을 요구한다 |
| `POST /api/issues?projectId=84` | 403 — "관리자가 API 토큰의 이슈 생성 권한을 비활성화했습니다" |
| `POST /api/sprints?projectId=84` | 401 — 스프린트 생성은 토큰 인증을 받지 않는다 |

`projectId`는 본문이 아니라 **쿼리스트링**으로 보낸다. 본문에 넣으면 "projectId가 필요합니다"가 나온다.

### 2026-08-28 진행 결과

관리자가 토큰의 이슈 생성 권한을 켠 뒤 **에픽 13건을 등록했다**(ST4F-3 ~ ST4F-15).
마감일은 각 스프린트 종료일로 넣었고, 라벨에 S1~S4와 에픽 ID를 넣었다.

| 토큰으로 되는 것 | 안 되는 것 |
|---|---|
| `GET /api/projects` | `GET /api/state`, `GET /api/issues`, `GET /api/sprints` — 전부 401 |
| `POST /api/issues` (생성) | `PUT`/`PATCH /api/issues` (수정) — 401 |
| `DELETE /api/issues` (삭제) | `POST /api/sprints` (스프린트 생성) — 401 |

즉 토큰은 이슈의 **생성과 삭제만** 한다. 조회와 수정은 로그인 세션을 요구한다.
정상 payload(`{name, start, end, goal}`)로 보내도 스프린트는 401이다.

그래서 스프린트 4개 생성과 에픽 배정은 [`teamflow_스프린트_생성.js`](teamflow_스프린트_생성.js)로 한다.
로그인한 브라우저의 콘솔에 붙여 넣어 실행하며, 이 페이지가 쓰는 API를 그대로 부른다.
같은 이름이 있으면 새로 만들지 않고, 지우는 동작이 없어 두 번 돌려도 안전하다.

★한글을 셸의 `curl -d`로 보내면 깨져서 저장된다(실측). Python 등에서 UTF-8로 직접 보낸다.
★수정이 안 되므로 잘못 넣으면 삭제 후 재생성해야 하고 이슈 키가 바뀐다.
